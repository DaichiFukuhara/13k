---
id: read.tell.telegraph
parent: read.tell
depth: 3
children: []           # 深さ3は葉
status: decision-approved   # telegraph@2026-09-05-decision-2（人間の承認 root@2026-09-04-approval-1）
seams: []
uses_seams: [r1.screen-state, t2.color-result]
---

# telegraph（システム: 危険範囲と発生時刻を、`s1` の値だけから描く）

## 📍 現行契約への索引

<!-- 2026-09-04 Codex 監査 P1-10: 「履歴が正本本文に大量に残り、
     『現在有効なもの』表を読んでも本文中の旧仮置きが現行命令の形で残るため
     cold start が安全でない」。現行だけを一意にたどれる入口をここへ置く。 -->

**この順に読めば現行の契約だけが揃う。**

1. **有効な版**: 決定 `telegraph@2026-09-05-decision-2` / 分割なし（深さ3の葉）
2. **現行の決定本文**: 1〜7（🔒 が付いた節が最新の凍結）
3. **承認証跡**: `### 現在有効なもの` 表 → その下の該当エントリ

> ⚠️ **`superseded` と書かれた節・`~~取り消し線~~`・「旧文は」で始まる引用は履歴である。**
> **現行の命令として読んではいけない。**


<!-- 深さ3（葉）。これ以上分割しない。

     ⚠️ このノードが不変条件8 の read 側の実効化点である。
     ここに座標計算を1行でも書いたら、設計が壊れる。 -->

## 親からの振り分け

- **責任**: 危険範囲と発生時刻を、`s1` の値だけから描く
- **詳細化の対象**: 親の本文1の機能「予告を出す」。根の 1.1（どこが危険かといつ発生するかが
  同時に出る）、1.7 の Y 軸（床付近か高さありか）
- **継承する制約**: 根12件＋`read` 3件＋`tell` 2件。とくに
  **8**（予告と当たり判定は同一定義）、**「`read` は危険範囲を計算しない」
  「色を決めるのは `palette` だけ」**。
  `s1` を読むだけ。描画順序（`tell` の親に残すもの4）。解像度（同5）。
  容量の目安 **1,400 inlined bytes**
- **割り当てられた受け入れ条件**: 予告と判定が一致する（主）、知覚の下限の検証（主）、
  初見（予告が読めること）、色を取り除いても遊べる（規則の実行）
- **uses_seams**: `r1.screen-state`, `t2.color-result`（`palette` から色と冗長化の指示）
- **提供する seam**: `t1.color-request`（`palette` へ）
- **parent_decision_ref**: `tell@2026-09-05-decision-3` ＋ `tell@2026-09-05-split-3`

## 1. これは何を決めるものか

**予備動作が始まってから攻撃判定が終わるまで、画面に何が出るか。**

### 1.1 描くもの

| 対象 | `s1` から使う値 | 描き方 |
| --- | --- | --- |
| **危険範囲** | **展開済みの矩形**（X, Y, 幅, 高さ） | 半透明で塗る ＋ 枠線 |
| **発生時刻** | 発生までの残りフレーム | **不透明度を上げていく**（充填） |
| **弾（SHOT）** | ボス位置と狙い先の Y | **射線**を引く（矩形が無いため） |
| **段** | 多段の何段目か | 現在段の矩形だけを出す |

**座標を1つも計算しない。**`s1` が運んできた矩形をそのまま塗る。

> ⚠️ **これがこのノードの全部である。**
> 実装の `drawTelegraph()` は `moveRect()` を呼ぶが、
> **`moveRect()` は判定側と共有された唯一の形状定義**である
> （[`design/AS_BUILT.md`](../../../../AS_BUILT.md) §3）。
> ツリーの設計ではその展開を `arena` が行い、**結果を `s1` に載せて渡す。**
> **`telegraph` は展開すら行わない。**

### 1.2 充填（「いつ」を色なしで示す）

**不透明度が予備動作の進行率に比例して上がる。**

| 段階 | 不透明度 |
| --- | ---: |
| 予備動作の開始 | 0.08 |
| 予備動作の終了直前 | 0.28 |
| **攻撃判定中** | **0.45** |

**色を使わない。**濃さだけで「いつ来るか」が読める（`read` の不変条件9）。

**出所は実装**（`drawTelegraph()`）。

### 1.3 「どこ」を色なしで示す

**矩形の位置と大きさだけで、どこが危険かが分かる。**

- 床付近の技（SWEEP）は**床に貼りつく薄い帯**として出る → ジャンプで越えられると分かる
- 高さのある技（SLAM）は**縦に厚い矩形**として出る → 越えられないと分かる
- 縦に落ちてくる技（RAIN）は**天井から床までの柱**として出る

**根の 1.7 の「床付近か高さありか」が、そのまま矩形の高さとして見える。**
**色を消しても、ジャンプで越えられるかが読める。**

### 1.4 色は `palette` から受ける（seam `t1` / `t2`）

**色値を直書きしない**（`tell` の不変条件8）。
`s1` の特性ベクトルを **`t1.color-request`** で `palette` へ渡し、
**`t2.color-result`** で返ってきた**色値と冗長化の指示**を使う。

> 🔒 **凍結（`telegraph@2026-09-05-decision-2`）。**旧文は「関数呼び出しであって seam ではない」
> と書いていたが、**親 `tell` の現行決定では `t1` / `t2` の seam である**
> （2026-09-04 Codex 監査 **P0-2**）。

## 2. なぜ要るのか

**根の不変条件8 の `read` 側の実効化点がここである。**

「予告した危険範囲と、実際の当たり判定は、同一の定義から作る。表示用の形を別に持たない」を
守る方法は2つある。

| 方法 | `telegraph` の扱い |
| --- | --- |
| 同じ関数を両方から呼ぶ | 実装がこれ（`moveRect()` の共有） |
| **片方が計算し、結果を渡す** | **ツリーはこれ。`arena` が展開し `s1` で渡す** |

**後者のほうが強い。**前者は「同じ関数を呼び忘れる」ことがありうるが、
後者は**`telegraph` に計算能力そのものが無い。**

## 3. 誰が使い、誰が影響を受けるか

- **審査員・投票者**。初見で最初に見るのがこれ
- **`palette`**: `t1.color-request` で色を要求し、`t2.color-result` で
  **色値と冗長化の指示**を受ける。**seam である**（`tell` の `split-2`）
- **`duel.fight.arena`**（親経由）: `s1` の危険範囲を作る側。
  **`arena` の展開が変わると `telegraph` の見た目が全部変わる**
- **`duel.gen`**（間接）: 知覚の下限（18F）が成立しないと `telegraph` が
  `boundary_request` を上げ、根が値を変え、生成が変わる

## 4. 終わったとどうやって分かるか（受け入れ条件）

- [ ] **予告と判定が一致する**（主担当）:
      **描画に渡した矩形が、`s1` の値とビット単位で同一。**
      判定: 全フレームで比較して差異0件

- [ ] **座標を計算していない**:
      **`telegraph` に算術演算による座標生成が1行も無い。**
      判定: コードで確認。`s1` のフィールドを読んで描画APIへ渡すだけであること。
      **例外は不透明度の計算だけ**（座標ではない）

      > 🧭 **これを受け入れ条件にしたのがこのノードの要点である。**
      > 「一致している」だけだと、**たまたま一致している**状態を許してしまう。
      > **計算能力が無いことを直接検査する。**（2026-09-04）

- [ ] **知覚の下限の検証**（主担当）:
      予備動作 18F で予告の出現を検出し種類を識別できる。同時2件でも読み分けられる。
      判定: **実際の予告語彙で**、18F の予告を10回提示し、
      制作者以外の1人が**8回以上正しく言える**こと。
      **不成立なら `boundary_request` で `tell` → `read` → 根へ返す**

- [ ] **色なしで「どこ」と「いつ」が読める**:
      グレースケール版で、**床付近の技と高さのある技を見分けられる**（ジャンプ可否の判断）。
      判定: グレースケール版で SWEEP と SLAM を各5回提示し、
      **ジャンプすべきか否かを8回以上正しく選べる**こと

- [ ] **容量**: **1,400 inlined bytes 以内**

## 5. 変えてはいけない前提

1. 根の不変条件 **8**（予告と当たり判定は同一定義。表示用の形を別に持たない）
2. **`read` は危険範囲を計算しない**（`read` の不変条件8）。
   **「描画のために少し補正する」こともしない**
3. **色を決めるのは `palette` だけ**（`tell` の不変条件8）。色値を直書きしない
4. **色を使わない手がかりを常に2つ以上出す**（`read` の不変条件9）。
   矩形（どこが）と充填（いつ）は色に依存しない
5. **描画順序**: 背景の次、弾より前（`tell` の親に残すもの4）
6. **`s1` を書き換えない**

## 6. 決めないこと・任せること

**実装が決めてよい**

- 不透明度の具体値（実装は 0.08 → 0.28 → 0.45）
- 枠線を描くか、太さ、点線か実線か
- 弾の射線の描き方（実装は直線）
- 充填の曲線（線形か、終盤で加速するか）
- 段が変わるときの見せ方

**今回は決めない**

- 危険範囲の形を変えること（`arena` が展開したものをそのまま使う）
- 予告に文字を出すこと（実装は技名を出しているが、**`frame` の HUD の担当**）

## 7. 分からないこと・仮置きしていること

1. **知覚の下限 18F は現状どの技も踏まない。**実装の最短は 24F。
   **検証用に 18F の技を手で作って測る**必要がある。
   仮置き: 検証専用のシードまたはデバッグ入力を用意する。`provisional: true`

2. **同時2件の読み分けを、実装で作れるか未検証。**
   実装は1体しか居ないので `wind` 中に別の技が始まらない。
   **同時2件が起きるのは弾が複数残る場合だけ**（`foe` の 7-4）。
   検証のために人為的に2件出す手段が要る

3. **充填の曲線が線形でよいか分からない。**線形だと「終盤の切迫感」が薄い可能性がある。
   実測していない

4. **`s1` を毎フレーム全部読むのか、差分かは `arena` が決める**（`arena` の 7-2）。
   `telegraph` はどちらでも動くが、**差分だと前フレームの状態を持つ必要が出て、
   「計算しない」が崩れる方向に働く**

## 承認証跡

| 種別 | 版参照 | status |
| --- | --- | --- |
| **決定** | `telegraph@2026-09-05-decision-2` | **active** |

**深さ3は子を作らないため、分割の承認は無い。**

- **`telegraph@2026-09-05-decision-2`** / 承認者: **人間（DaichiFukuhara、`root@2026-09-04-approval-1` により）**/
  日時: 2026-09-05 / 種別: `decision` / 承認者の種別: **人間** / **status: active**

  **決定の要点**: 受け入れ条件に「**座標を計算していない**」を入れた。
  「予告と判定が一致する」だけだと**たまたま一致している状態を許す**ので、
  **計算能力が無いことを直接検査する。**

  実装は `moveRect()` を判定と共有する形（同じ関数を両方から呼ぶ）だが、
  **ツリーは `arena` が展開して `s1` で渡す形**にした。
  **後者のほうが強い** — 呼び忘れが起きえない。

  **2026-09-04、人間が `root@2026-09-04-approval-1` で明示的に承認した。**
  `waiver-1`（AI への委任）は深さ3到達をもって終了している。
