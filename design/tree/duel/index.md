---
id: duel
parent: root
depth: 1
children: []
status: draft          # draft / questions-done / decision-approved
                       # / split-proposed / split-prechecked / split-approved
                       # / children-created / done
seams: []              # 子同士の接続。子がいなければ空
uses_seams: [s2.read-floor, s4.size-verdict]
---

# duel（課題: 生成された敵と1人のプレイヤーが噛み合わず、回避不能な攻撃が出る）

<!-- 深さ1。このチャットで決めるのは「課題の範囲と、その課題に対する解決法」。
     まだ何も決まっていない（status: draft）。roles/interview.md の固定質問7つから始める。

     このノードは cold start で開くこと。渡す文言:

       design/harness/README.md に従って design/tree/duel を進めてください。
       閉包（自ノードと祖先チェーンの index.md）を先に読んでください。

     兄弟（read / ship）の index.md は読まない。兄弟との接続は下の uses_seams と、
     親（design/tree/index.md）の分割提案 split-1 の seam 節にすべて書かれている。 -->

## 親からの振り分け

> ⛔ **この節は 2026-09-01 に失効した。このノードをまだ進めないこと。**
>
> 親の**決定の再承認（`decision-3`）と分割の再承認（`split-4`）**が済むまで、
> ここに書かれた責任・制約・`uses_seams` を前提にしてはいけない。
>
> 経緯: Codex の前検査3回。1回目（`split-1` 対象）重大5件・「修正のうえ再提案」、
> 2回目（`split-3` 対象）重大8件・「判定不能」、3回目（`split-4` 対象）重大8件・「判定不能」。
> 全文は親 `design/tree/index.md` の各 `#### precheck` にある。
> **現時点で有効な分割提案は無い**（`split-1` superseded / `split-2` withdrawn /
> `split-3` rejected / `split-4` pending）。
>
> **`duel` に効く重大指摘:**
>
> - **重大2**: `s2.read-floor` の値が未決定で、開く順序も未定だった。このままでは
>   `duel` は生成制約を確定できない。`split-2` は親が暫定初期値（最低予備動作長 18F /
>   同時予告上限 2）を置き、開始順を **`ship`第1段 → `read` → `duel` → `ship`第2段** に固定した
> - **重大5**: 親の本文1（作品定義）に「交代」が残っており、1.4「4つだけ」と矛盾していた。
>   **`duel` が cold start した時点で「交代を実装するのか」が一意に読めない状態だった。**
>   親の本文を修正済み（`decision-2` として再承認待ち）
> - **重大3**: `s1` が `s1.presentation-state` へ拡張され、`duel` が送る内容が増える
>   （戦闘状態・ライフサイクル・シード・技構成・操作割当）
> - **重大4**: `s7.canonical-source`（`ship` から）が追加され、`uses_seams` が3本になる
>
> **2026-09-01 追記: 人間が案 B を選択した。**差し替え案は親の `## 分割提案 split-3`
> （status: pending）。`split-2`（案 A）は withdrawn。`duel` は B でも残るが、
> **`ship` が消えるので `uses_seams` は `s2.read-floor` の1本だけ**になり、容量 5.6〜7.5KB は
> 暫定から**確定値**へ変わる（超過したら `boundary_request` で根へ返す）。
> 開始順は **`read` → `duel`** で `duel` は2番目。
> **人間の承認後、親が振り分けを転記し直すまで待つ。**

<!-- 親（root）が split-1 の承認時に転記した。子は書き換えない。
     間違っていると判断したら、自分で直さず boundary_request で root へ返す。

     注記: 項目が README の6項目より多い（「提供する seam」を足している）。
     これは AIDE 本体側で既に root へ boundary_request が起票されている
     既知の差分（precedents.md 2026-08-31「bootstrap 例外が終了した」）。
     受信側しか列挙しない uses_seams だけでは、送り手に失効が届かないため。 -->

- 責任: 生成された敵と1人のプレイヤーが噛み合い、どの技にも必ず抜け道がある状態を作る
- 詳細化の対象: 1.2（生成する/しないの境界）、1.3（危険度予算と禁止組み合わせ）、
  1.4（回答手段は最低2種類）、1.5（資源はスタミナ1本）、1.8（ランの構造・仮置き）
- 継承する制約: 根の不変条件12件すべて。特に 7（戦闘中の判定を生成しない）、
  9（同じシードは同じ敵）、10（反射神経に依存しない回答を最低1つ持つ）。
  形状定義の正本を持つ側としての書き込み権限（親に残すもの3）。
  容量 5.6〜7.5KB（暫定・親に残すもの4）
- 割り当てられた受け入れ条件: 学習（主）、初見（前提としての公平性）
- uses_seams: `s2.read-floor`（read から: 読むために必要な最低予備動作長と、同時に出せる予告の上限。
  生成の制約として受ける）、`s4.size-verdict`（ship から: 割り当て容量と、超過時に落とすものの指示。
  受けた部分の決定は失効する）
- 提供する seam: `s1.attack-payload`（read へ）、`s3.size-claim` / `s5.cut-refusal`（ship へ）
- parent_decision_ref: `root@2026-08-31-decision-1`（本文1〜7）
  ＋ `root@2026-08-31-split-1`（分割・振り分け・親に残すもの・共通語彙・容量の暫定配分）

## 1. これは何を決めるものか

<!-- 深さ1: 課題の範囲と、それに対する解決法 -->

## 2. なぜ要るのか

## 3. 誰が使い、誰が影響を受けるか

## 4. 終わったとどうやって分かるか（受け入れ条件）

<!-- 親から割り当てられた条件を、このノードで検査可能な形にする。
     親条件の写しにしない。判定者と照合基準を書く。
     ここが空のまま決定承認または split へ進んではいけない -->

- [ ]

## 5. 変えてはいけない前提

<!-- 上の「継承する制約」がここへ降りてくる。継承したものは出所を書く -->

## 6. 決めないこと・任せること

<!-- 親の 6（決めないこと）のうち、自分の担当ぶんをここで具体化する。
     ここが空だと実装者が止まり続ける -->

## 7. 分からないこと・仮置きしていること

## 分割提案

<!-- 決定承認後に roles/split.md の形式で追記する。深さ1の子は深さ2（解決法）。
     前検査は提案者と別実装の AI が行う -->

## boundary_requests

<!-- 子孫からの境界変更の起票。追記のみ -->

## seam_inbox

<!-- 子同士の seam 成果物はここへ追記され、親が権威化して宛先の子へ降ろす -->

## 承認証跡

<!-- 未承認。決定の承認が入ったら duel@<日付>-decision-1 として記録する -->
