---
id: duel
parent: root
depth: 1
children: [gen, fight]
status: children-created   # 決定 duel@2026-09-05-decision-8 / 分割 duel@2026-09-05-split-5。
                       # いずれも waiver-1 による委任承認。振り分けの転記まで完了。
                       # duel-split-2 は前検査を経ていない（Codex がリソース上限）。
                       # 理由は承認証跡へ記録した。duel-split-1 は「判定不能」→ superseded
seams: [d1.run-definition]   # 子（gen -> fight）の片方向1本
                       # 旧 d1.boss-definition（1体分）は、fight -> gen の輪を生むので廃止した
uses_seams: []         # 親が定義した seam のうち、自分が使うもの（duel は受信しない）
---

# duel（課題: 生成された敵と1人のプレイヤーが噛み合わず、回避不能な攻撃が出る）

## 📍 現行契約への索引

<!-- 2026-09-04 Codex 監査 P1-10: 「履歴が正本本文に大量に残り、
     『現在有効なもの』表を読んでも本文中の旧仮置きが現行命令の形で残るため
     cold start が安全でない」。現行だけを一意にたどれる入口をここへ置く。 -->

**この順に読めば現行の契約だけが揃う。**

1. **有効な版**: 決定 `duel@2026-09-05-decision-8` / 分割 `duel@2026-09-05-split-5`
2. **現行の決定本文**: 1〜7（🔒 が付いた節が最新の凍結）
3. **現行の分割**: `## 分割提案` 節（見出しの版が上の分割と一致するものだけが有効）
4. **承認証跡**: `### 現在有効なもの` 表 → その下の該当エントリ

> ⚠️ **`superseded` と書かれた節・`~~取り消し線~~`・「旧文は」で始まる引用は履歴である。**
> **現行の命令として読んではいけない。**


<!-- 深さ1。このチャットで決めるのは「課題の範囲と、その課題に対する解決法」。

     このノードは cold start で開くこと。渡す文言:

       design/harness/README.md に従って design/tree/duel を進めてください。
       閉包（自ノードと祖先チェーンの index.md）を先に読んでください。

     兄弟（read）の index.md は読まない。read との接続は、自分が提供する
     s1.presentation-state だけであり、その内容は親（design/tree/index.md）の
     分割提案 split-6 の seam 節に全部書かれている。

     duel は最初に開くノードである。read は duel の s1 が確定してから開く。 -->

## 親からの振り分け

<!-- 親（root）が split-6 の承認時に転記した（root@2026-09-03-children-created）。
     子は書き換えない。間違っていると判断したら、自分で直さず boundary_request で root へ返す。

     注記: 「提供する seam」と「開く順序」は README の6項目に無い追加項目。
     uses_seams は受信側しか列挙しないため、これが無いと送り手へ失効が届かない
     （root@2026-08-31-decision-4 の「両端」規則）。AIDE 本体側へ起票済みの既知の差分。 -->

- **責任**: 生成された敵と1人のプレイヤーが噛み合い、どの技にも必ず抜け道がある状態を作る

- **詳細化の対象**:
  - `1.2` 生成する / しないの境界
  - `1.3` 危険度予算・特性と代償の軸・**危険包絡と安全席の検査器**
  - `1.4` 回答手段は最低2種類
  - `1.5` 資源はスタミナ1本
  - `1.7` の Y 軸（攻撃が床付近か高さありか）
  - `1.8` ランの構造（仮置き）
  - `1.10` **技の奪取**（プレイヤー版への変換規則とスタミナ消費の式）

- **継承する制約**: 根の不変条件12件すべて。特に
  - **7** 戦闘中の判定を生成しない
  - **9** 同じシードは同じ敵を再現する
  - **10** すべての生成攻撃は、反射神経に依存しない回答を最低1つ持つ
  - 形状定義の**正本を持つ側**としての書き込み権限（親に残すもの3）
  - **知覚の下限**（最低予備動作長 18F・同時予告上限 2）は根が置いた**仮の前提**
    （`provisional: true`・親に残すもの4）。生成の制約として受ける。
    値の検証は `read` の担当なので、`duel` はこの値を疑わずに使う
  - 容量の目安 **18,500 inlined bytes**（設計時見積もり・親に残すもの9）。
    実測と再調停は実装段階へ `deferred`
  - **実装基盤は `prismatic-duel/game.js`**（2026-09-04・人間の決定。根の本文 6.1・親に残すもの11）。
    ~~`deferred`（再開条件: 最初の深さ2の分割承認まで）~~。
    **土台にするのはコードと build 環境**であって `prismatic-duel` のゲーム設計ではない —
    設計はこのツリーが正本であり、合わない実装は捨てる

- **割り当てられた受け入れ条件**: 学習（主）、初見（前提としての公平性）

- **uses_seams**: なし（`duel` は兄弟から何も受け取らない）

- **提供する seam**: `s1.presentation-state`（`read` へ）。
  内容は親の `split-6` の seam 節にある。**危険範囲の正本は `duel` が持ち、`read` は読むだけ**

- **開く順序**: **1番目**（このノードから始める）

- **parent_decision_ref**: `root@2026-09-05-decision-11` ＋ `root@2026-09-05-split-10`
  <!-- 2026-09-04: decision-6 / split-6 から更新。実装基盤の deferred 解除により版が切り直された -->

<!-- 2026-09-04: 上の1行を除き、この節は root の split-7「子ごとの振り分け」からの
     そのままの転記である（Codex split-1 前検査 軽微1 の型を避けるため、版参照も一致させた） -->

## 1. これは何を決めるものか

<!-- 深さ1: 課題の範囲と、それに対する解決法 -->

**課題の範囲**: シードから生成された敵と、1人のプレイヤーが**1戦のあいだ噛み合う条件**と、
それを**構造として保証する仕組み**を決める。

含むもの: 何を生成し何を生成しないかの実効化 / 危険度予算と禁止組み合わせ /
危険包絡と安全席の検査 / プレイヤーの4つの回答手段とスタミナ / 当たり判定 /
ボスの行動選択 / ランの構造。

含まないもの: 画面に何をどう出すか（`read` の担当）/ 具体的な数値（深さ2以降へ委任）。

### 解決法: 「公平さを祈らず、生成のたびに検査する」

根の `1.3` は公平性を**危険度予算**で担保しようとしていたが、
Codex の前検査で**予算だけでは不変条件10（反射神経に依存しない回答を最低1つ持つ）を
保証できない**ことが分かった。予算は「強さの総量」を揃えるだけで、
「抜け道があるか」は測っていないからである。

そこで `duel` は、**保証を予算から検査へ移す**。

| 層 | 役割 | 保証するもの |
| --- | --- | --- |
| 1. **危険度予算** | 技の強さの総量を揃える | ボスごとの**難易度の一貫性** |
| 2. **禁止組み合わせ** | 検査を通りにくい組を最初から作らない | **引き直し回数の削減**（保証ではなく最適化） |
| 3a. **安全席の検査** | **黄の技**が、静止したまま避けられることを確かめる | 根の 1.3（黄が射程＝危険包絡で払うこと） |
| 3b. **回答2つの検査** | **全技**が、有効な回答手段を2つ以上持つことを確かめる（うち1つは反射神経を要さない） | **不変条件10 と 1.4**（公平性の本体） |
| 4. **選択時の条件** | その技を「今」出してよいかを、プレイヤーの位置で判定する | **利用可能性**（席が存在するだけでなく、座れていること） |
| 5. **フォールバック** | 引き直し上限（8回）を超えたら固定技へ | **停止性**（特定シードで生成が終わらないことの防止） |

**この5層が `duel` の解決法の骨格である。**1〜2 は既に根が書いており、
`duel` が新しく引き受けるのは **3・4・5 を実装可能な形へ落とすこと**。

> 🧭 **AI**: 2026-09-04、3 を **3a / 3b に分けた**（Codex `duel-split-1` 前検査 重大6）。
> **初稿は 3 を「安全席の検査」1つにして、それが不変条件10 を保証すると書いていた。**
> しかし安全席が証明するのは**回答1つ**（静止して避けられること）だけで、
> 1.4 の「最低2種類」には届かない。しかも根が安全席を課しているのは**黄の技だけ**である。
> **保証すべき命題が2つあったのに、検査は1つしか無かった。**（2026-09-04）

### 生成は決定論的でなければならない

不変条件9（同じシードは同じ敵を再現する）は、**検査と引き直しを含めて決定論的**
であることを要求する。引き直しが乱数の状態を進めるので、
**シードから生成器の全状態が再現できる**必要がある。

- 生成に使う乱数は**シードから導出した1本の系列**だけ。時刻・端末・実行回数に依存しない
- 検査の結果も決定論的（浮動小数の丸めに依存しない形で判定する）
- **リトライ時は生成をやり直さない。**生成結果をそのまま再利用する（不変条件9）

## 2. なぜ要るのか

**今どうなっているか**: 根は「生成された攻撃には必ず抜け道がある」と宣言しているが、
**それを確かめる主体が居ない。**根の `1.3` は禁止組み合わせを列挙する形だったが、
Codex の前検査3回で**列挙では塞ぎきれない**ことが繰り返し示された
（黄×藍、黄×紫、接近後攻撃、一度だけ照準を合わせる技、安全席が排他的な組）。

**何が困るか**: 抜け道の無い技が1つでも生成されると、
**受け入れ条件「初見」（なぜ負けたかを自分の言葉で言える）が壊れる。**
プレイヤーは「理不尽だった」としか言えず、それは学習ではない。
しかも生成物なので、**制作者がテストで踏むとは限らない。**
特定のシードでだけ壊れる不具合になり、審査員が引く可能性がある。

**祈りでは足りない。**列挙を増やすたびに新しい抜け道が見つかったのが、前検査3回の記録である。

## 3. 誰が使い、誰が影響を受けるか

**使う人**

- **js13kGames の審査員・投票者**。初見・短時間。1体目のボスで理不尽を踏んだら閉じる
- **リトライするプレイヤー**。同じ敵が再現されることを前提に学習する

**影響を受けるもの・人**

- **`read`**。`s1.presentation-state` の正本を `duel` が持つ。
  `duel` が形状定義を変えると `read` の予告が全部ずれる
- **制作者本人**。検査器は生成器と同じくらいのコード量になりうる。18,500 bytes の中で両立させる
- **将来の実装者**。検査を通らない技を捨てる設計は、**生成が確率的に遅くなる**。
  上限とフォールバックが無いとフリーズする

> 💬 **AI**: 3 に `read` を入れたのは、`s1` の正本が `duel` にあるからである。
> `duel` の決定は**兄弟の設計を直接壊せる**唯一の位置にある。（2026-09-03）

## 4. 終わったとどうやって分かるか（受け入れ条件）

親から `学習（主）` と `初見（前提としての公平性）` を割り当てられた。
**親条件の写しではなく、`duel` で検査可能な形にする。**

- [ ] **決定論**（親条件「学習」の前提）:
      **同一シードから2回生成した結果が、ビット単位で一致する。**
      技構成・数値・修飾子・山札・外見・第2形態変異のすべてを含む。
      判定: 1,000 個のシードで2回ずつ生成し、シリアライズしたバイト列を比較して全一致

- [ ] **公平性 A: 安全席**（親条件「初見」の前提）:
      **生成されたすべての黄の技が、安全席の検査を通っている。**
      判定: 10,000 個のシードでボスを生成し、**黄の技すべて**について
      「危険包絡をプレイヤー半幅で膨張させ、画面内の有効中心位置から差し引いた残りが
      プレイヤー1人分以上の幅を持つ」ことを検査して、**違反0件**

  > 🧭 **AI**: 2026-09-04 修正（Codex `duel-split-1` 前検査 **重大6**）。
  > **旧文は「生成されたすべての技」と書いており、根の 1.3 の範囲を変更していた。**
  > 根が安全席の検査を課しているのは**黄の技だけ**である（1.3「黄の技は、生成後に次を検査する」）。
  > 根の 1.3 の🧭は「**移動して避けるしかない技は黄にできない**」と書いており、
  > そういう技が存在すること自体は許している。
  >
  > 全技に「動かずに安全な席」を要求すると、**移動して避ける技・ジャンプで越える技・
  > ロールで抜ける技が1つも生成できなくなる。**根の 1.4 が挙げる回答手段4つのうち
  > 3つが死ぬので、これは詳細化ではなく**別のゲームへの変更**だった。（2026-09-04）

- [ ] **公平性 B: 回答が2つ**（親条件「初見」の前提・不変条件10 と 1.4 の本体）:
      **生成されたすべての技が、有効な回答手段を2つ以上持ち、そのうち1つは
      反射神経を要求しない。**
      判定: 上の 10,000 シードで、全技について
      **プレイヤー能力契約（本文5 の 12）に対する到達可能性を機械的に評価**し、
      「反射神経を要求しない回答（早めの移動・距離を取る・ジャンプで越える）が1つ以上」かつ
      「回答が合計2つ以上」を満たすこと。**違反0件**

  > 🧭 **AI**: **これが `duel-split-1` に無かった条件である**（Codex 重大6 の後半）。
  > 安全席は回答を**1つ**しか証明しない。根の 1.4 は「最低2種類」を要求しており、
  > 旧文はそれを検査していなかった。
  >
  > **A と B は別物である。**A は静止して避けられることの保証（黄だけ）。
  > B は回答が2つあることの保証（全技）。A を満たさない技も B は満たせる —
  > たとえば「予備動作が長く、ジャンプで越えられ、ロールで抜けられる技」は
  > 安全席を持たなくてよい。（2026-09-04）

- [ ] **停止性**:
      **どのシードでも生成が有限時間で終わる。**
      判定: **構造上の根拠**（引き直し上限8回＋必ず検査を通る固定フォールバック技）を
      コードで確認し、加えて上の 10,000 シードで無限ループ・例外が0件

- [ ] **利用可能性**（選択時条件の実効化）:
      **黄の技が選ばれた瞬間、プレイヤーは必ずその技の安全席の中に居る。**
      判定: 自動プレイ（ランダム入力）で 1,000 戦を走らせ、
      黄の技が発動した全フレームについて、発動時のプレイヤー中心が安全席内にあったことを記録。違反0件

- [ ] **学習が起きる**（親条件「学習」の本体・`duel` 側の分担）:
      **根の判定方法をそのまま使う** —
      **同一シードへの連続挑戦を記録し、1回目と3回目の被弾数を比べて減っている。**
      判定: 根の 4「学習」のまま。実施者は制作者でよい（「初見」と違い、
      根は被験者が制作者以外であることを要求していない）

  - [ ] **（補助）台本比較**: 上を**設計段階で先読みする**ための自動検査。
        同一シードのボスに対し、(a) 完全ランダム入力と (b) 技の山札を知っている台本入力を
        各20回走らせ、**(b) の平均被弾回数が (a) の 50% 以下**であること。
        **これは根の条件の代用ではなく、早期警告である。**
        不成立なら「技を覚えても勝率が上がらない」ので設計を直す

> 🧭 **AI**: 2026-09-04 修正（Codex `duel-split-1` 前検査 **重大7**）。
> **旧文は根の判定方法（1回目と3回目の被弾比較）を台本比較へ差し替えていた。**
> 根の `親に残すもの6` は「子は自分に割り当てられた条件を**検査可能な形へ具体化**するが、
> **条件そのものを書き換えない**」と定める。台本比較が証明できるのは
> 「**知識を持つ入力が有利になり得ること**」であって、
> **人間の学習が実際に起きること**ではない。別の命題だった。
>
> 根の条件を主に戻し、台本比較は**補助（早期警告）**へ降格した。
> 台本比較には人間の被験者が要らないという利点があるので、捨てはしない。（2026-09-04）

## 5. 変えてはいけない前提

**根から継承**（`root@2026-09-03-decision-6` / 親からの振り分け）

1. **不変条件12件すべて。**とくに 7（戦闘中の判定を生成しない）、
   9（同じシードは同じ敵）、10（反射神経に依存しない回答を最低1つ持つ）
2. **形状定義の正本は `duel` が持つ**（親に残すもの3）。`read` は読むだけ。
   **`duel` は表示用の別定義を作らせない責任も負う**（不変条件8の実効化）
3. **知覚の下限**: 最低予備動作長 18F / 同時予告上限 2（`provisional: true`・親に残すもの4）。
   **`duel` はこの値を疑わずに使う。**検証は `read` の担当
4. **回答手段は4つだけ**（移動 / ジャンプ / ローリング / パリィ）。増やさない
5. **資源はスタミナ1本**（1.5）。残機・控え・交代を作らない
6. **X = 地上の移動軸 / Y = ジャンプ中のみの回避軸 / Z = 無し**（1.7）。空中制御を持たせない
7. **容量の目安 18,500 inlined bytes**（設計時見積もり・親に残すもの9）
8. **実装基盤は `prismatic-duel/`**（2026-09-04・人間の決定。根の本文 6.1）。
   土台にするのは**コードと build 環境**であって `prismatic-duel` のゲーム設計ではない

13. **奪取技の変換規則**（根の 1.10 の実効化。`duel` がここで決める）

    **倒したボスの技を、プレイヤーの攻撃へ写す規則。**両子が同じ規則を使う。

    | 引き継ぐ | 捨てる／置き換える |
    | --- | --- |
    | 形状（6種のどれか） | **威力** → プレイヤー固定の `7`（ボスの威力1〜4は使わない） |
    | 射程（`REACH` の段） | **追尾** → **捨てる**（プレイヤーは標的を持たない） |
    | 予備動作（＝発生までのフレーム） | **回答手段の宣言** → 意味を持たない（ボスが避けるわけではない） |
    | 攻撃後硬直 | **危険度** → 意味を持たない |
    | 多段（段数と段間隔）**ただし1段の威力は `7 / 段数`** | |

    **多段の1段あたり威力**: `7 / 段数`（切り捨て。**合計は常に 7**）。
    段数が増えても総ダメージは変わらない。**体勢の削りだけが段数ぶん有利になる**

    **スタミナ消費**: **ボスと同じ危険度式で決める。**

    ```
    奪取技の危険度 = threat(威力を正規化段へ固定、追尾を0にした技)
    スタミナ消費   = clamp(10 + 危険度 × 2, 12, 40)
    ```

    | 例（射程 / 予備動作 / 硬直 / 段） | 危険度 | 消費 | 100で何回 |
    | --- | ---: | ---: | ---: |
    | 48px / 80F / 76F / 1段 | −1 | **12** | 8.3 |
    | 82px / 54F / 32F / 1段 | 6 | 22 | 4.5 |
    | 136px / 36F / 50F / 1段 | 8 | 26 | 3.8 |
    | 48px / 24F / 18F / 1段 | 10 | 30 | 3.3 |
    | **250px / 24F / 18F / 1段** | 15 | **40** | **2.5** |
    | 250px / 24F / 18F / 3段 | 19 | 40 | 2.5 |

    **素手は消費 14 に据え置く**（危険度から外れる固定値）。

    > 🔒 **凍結（`duel@2026-09-05-decision-8`）。**
    >
    > **旧文は消費を「予備動作だけの関数」にしていたが、それでは 1.8 を守れない。**
    > **射程と多段が代償を持たず、実質的な強化になる** —
    > 250px の SHOT を持てば一方的に削れ、多段3なら1回の消費で3倍当たる。
    > 「威力を正規化したから成長ではない」は**威力についてしか成立していなかった。**
    >
    > **ボスと同じ危険度式を使う。**`threat()` は射程・予備動作・硬直・多段・持続を
    > すべて1つの数へ畳む式で、**既に 30,000体で運用されている。**
    > 奪取技をその式で測り、消費へ写せば:
    >
    > | 何が起きるか | どう払うか |
    > | --- | --- |
    > | 長射程 | 消費が上がる（`RANGE_COST` が加点） |
    > | 速い | 消費が上がる（`WIND_COST` が加点） |
    > | 多段 | 消費が上がる（`(段−1)×2` が加点） |
    > | 長い硬直（隙が大きい） | **消費が下がる**（`REC_COST` が減点） |
    >
    > **「強い技ほど撃てない」が1つの式で成立する。**
    > 実装も `threat()` の再利用で済むので容量が増えない。
    >
    > **多段の1段あたり威力を割るのは、手数の強化を消すため。**
    > 総ダメージが変わらないので、多段は「体勢を削りやすいが1発が軽い」という性格になる。
    >
    > ⚠️ **`SHOT` を奪った場合の扱いは深さ3で決める**（`avatar` の 6）。
    > プレイヤーが弾を撃つと、`arena` の接触解決が「プレイヤーの弾」を扱う必要がある。
    > 実装には既に `shots` の `owner` があるので経路はある

**`duel` 自身の不変条件**（このノードで著述）

9. **公平性の保証は検査が持つ。予算と禁止列挙は最適化にすぎない。**
   子は「予算で釣り合っているから安全」と論じてはいけない
10. **生成は決定論的**（1 の後段）。引き直しを含めてシードから再現できる
11. **検査を通らない技は必ず捨てる。**「ほぼ安全」で通さない
12. **プレイヤー能力契約**（`provisional: true`。`duel` がここで置く仮の値）

    公平性の検査は**プレイヤーに何ができるか**を知らないと成立しない。
    「ジャンプで越えられる」と宣言するには、ジャンプで到達できる高さが要る。
    **この量は生成側と戦闘側の両方が使うので、`duel` が持つ。**

    > 🔒 **凍結（`duel@2026-09-04-decision-3`）。値は実測である。**
    > 2026-09-04 の Codex 監査（**着手前7**）で、
    > **旧表の値が AI の直感で置かれたもので、実測と衝突している**ことが指摘された。
    > `avatar` の `boundary_request` を**受理**し、**実測値へ差し替えた。**

    | 量 | **確定値**（`prismatic-duel/` の実測） | 旧・仮の値 | 誰が使うか |
    | --- | ---: | ---: | --- |
    | プレイヤー当たり判定の**半幅** | **6 px** | 9 | `gen`（包絡の膨張）/ `fight`（衝突） |
    | プレイヤー当たり判定の**幅 × 高さ** | **12 × 25 px** | 高さ 44 | 同上 |
    | ジャンプの**最高到達高さ**（足裏基準） | **59 px** | 52 | `gen`（床付近か高さありかの判定）/ `fight` |
    | ジャンプの**滞空フレーム数** | **35 F** | 34 | 同上 |
    | ジャンプ中に**無敵でない**こと | — | — | 越えるのは高さであって無敵ではない |
    | ローリングの**無敵フレーム数** | **11 F**（全体24F の 4〜14F） | 11 | `gen`（回答になるかの判定）/ `fight` |
    | ローリングの**総フレーム数 / 移動距離** | **24 F / 110〜139 px** | 26 / 96 | 同上 |
    | パリィの**受付フレーム数** | **6 F**（全体22F の 4〜9F） | 6 | 同上 |
    | 歩行速度 | **2.45 px/F** | 3.1 | `gen`（早めの移動が間に合うかの判定）/ `fight` |
    | 画面の**内部解像度** | **640 × 360**（16:9） | 480×270 | 両方 |
    | 床の Y / 戦闘領域の X 範囲 | **307** / **X ∈ [20, 602]** | X ∈ [24, 456] | 両方 |
    | ボス当たり判定 | **37 × 68 px** / X ∈ [45, 575] | — | 両方 |
    | フレームレート | **60 fps 固定** | 60 | 両方 |
    | 被弾後の無敵 | **42 F** | — | `gen`（多段の2段目以降が当たるかの判定） |
    | **プレイヤーの最大 HP** | **14** | — | `fight`（`f1` で運ぶ）/ `read`（HUD の分母）/ **`gen`（合計予算の基準）** |
    | **プレイヤーの最大スタミナ** | **100** | — | 同上 |

    > 🔒 **最大 HP を 14 に凍結した**（`duel@2026-09-05-decision-8`）。
    >
    > 2026-09-05 の再監査（**重大7**）: 「初稿は実装の UNICORN の値 **8** をそのまま置いたが、
    > **実装は HP 8/10/7 の3人で合計 25、死亡時に自動交代する。**
    > 1人 HP 8 は生 HP だけで**約68%減**。
    > `fight` はこの難化をボスの危険度予算で吸収するとしているが、
    > **`gen` は合計上限の値を未決定のまま委任しており、対応式も基準も無い。**
    > 暫定値で済む差ではなく、**ゲームを別難易度にする入力。」**
    >
    > **HP と予算の両方を同時に決める。**
    >
    > | | 実装（3人交代） | 凍結値（1人） | 根拠 |
    > | --- | ---: | ---: | --- |
    > | 生 HP の合計 | 25 | **14** | 交代の無敵・自動交代の猶予が消えるぶん、単純比では足りない。**56%** に置く |
    > | **ボス合計危険度の上限** | 無し | **`24 + tier × 6`**（24 / 30 / 36） | 下記 |
    >
    > **合計上限の導出**: 1体の技数は4〜5、通常技の帯域は `[3+tier, 6+tier]`。
    > **全技が帯域上限を取ると `5 × (6+tier)` = 30 / 35 / 40** になる。
    > 上限をその **8割**（24 / 30 / 36）に置くと、**全技が上限を取ることはできず**、
    > 強い技と弱い技が混ざる。**これが「技が全部強い個体」を防ぐ層である**（根の 1.3）。
    >
    > **判定基準**: `fight` の受け入れ条件「学習が起きる（補助・台本比較）」で
    > **台本入力の平均**被ダメージ**が HP 14 の 50% 未満**（＝7ダメージ未満）に収まること。
    > 超えたら**予算の上限を下げる**（HP は動かさない — `audit B` が使わない値で
    > 難易度を調整すると、公平性の検査に掛からない層で難易度が動く）。
    >
    > ⚠️ **「7発未満」は誤り**（2026-09-05 3回目の監査）。
    > **ボスの1発は威力1〜4なので、7発と7ダメージは違う。**
    > 威力2平均なら**4発で7ダメージ**に達する。**被ダメージで測る。**
    >
    > **合計上限の導出も直した。**旧文は `5 × (6+tier)` を全技上限としたが、
    > **1体目は4技**であり、**固有技は別帯域**（`[6+tier, 8+tier×2]`）である。
    >
    > | `tier` | 技構成 | 全技が上限を取ったときの合計 | `budgetCap` | 割合 |
    > | ---: | --- | ---: | ---: | ---: |
    > | 0 | 通常3 ＋ 固有1 | `3×6 + 8` = **26** | 24 | 92.3% |
    > | 1 | 通常4 ＋ 固有1 | `4×7 + 10` = **38** | 30 | 78.9% |
    > | 2 | 通常4 ＋ 固有1 | `4×8 + 12` = **44** | 36 | 81.8% |
    >
    > **tier 0 だけ上限が緩い**（92%）が、1体目は防御能力を持たず技も4つなので許す。

    #### 撃破時間（プレイヤーの攻撃性能の根拠）

    **プレイヤーの攻撃は `dmg 7 / cost 14 / 発生9F / 全体22F`。**
    スタミナ制約（100・消費後30F停止・0.3/F 回復）込みで測ると:

    | `tier` | ボス HP | 必要な攻撃回数 | 撃破時間 |
    | ---: | ---: | ---: | ---: |
    | 0 | 120 | 16 | **約 14 秒** |
    | 1 | 160 | 21 | **約 20 秒** |
    | 2 | 210 | 28 | **約 29 秒** |

    （反撃窓の 1.25 倍を4割の攻撃で取れた場合。取れないと 19/26/36 秒）

    > 🔒 **凍結（`duel@2026-09-05-decision-6`）。**
    > 3回目の監査が **`dmg 4` では 30/40/53 回・最大 67.7 秒/体**と実測し、
    > 「スタミナ 100 では連続6回で枯れ、以後1攻撃ごとに約84F待ち」と指摘した。
    > **1体で1分を超えると初見の集中が切れる。**
    > `dmg` を 7、`cost` を 14 へ上げて **14〜29 秒/体**に収めた。
    > **これは「1体あたりの目標時間」（根の 6 が下位へ委任）の実質的な決定である。**
    >
    > **最大スタミナ 100 は公平性の検査に効かない**（`audit` は使わない）が、
    > `s1` の完全性のために契約が持つ。

    **子（深さ2以降）は、この値を ±25% の範囲で調整してよい。**
    範囲外へ出すか、量そのものを増減させる場合は `boundary_request` で `duel` へ返す。
    **調整したら、受け入れ条件「宣言との一致」を再検査する**（`fight` の不変条件10）。

    > ⚠️ **ロール無敵 11F とパリィ受付 6F は、旧・仮の値と偶然一致していた。**
    > 他が全部ずれていたので、**一致した2件も検証の結果ではない。**（2026-09-04）

    > 🧭 **AI**: 2026-09-04 追加（Codex `duel-split-1` 前検査 **重大2・4**）。
    > **`duel-split-1` は「共有される定数は seam ではなく親の決定にする」という
    > 根で効いた型を適用したつもりだったが、当たり判定サイズしか移していなかった。**
    > `gen` が渡す「回答手段の宣言」を検証するには、ジャンプの高さ・滞空・ロールの無敵・
    > 歩行速度が要る。それらを `fight` に残したままにすると、
    > **`fight → gen` の逆向き依存が生まれる**（宣言が正しいかは `fight` が決まるまで分からない）。
    >
    > **値まで書いたのは、`precheck.md` §1「親に残すものは項目名の列挙では足りない」**
    > に従ったからである（重大2）。ただし数値は `provisional` で、
    > **根が知覚の下限（18F / 同時2件）でやったのと同じ形**にした —
    > 親が仮の値を置き、子は範囲内で調整し、成立しなければ `boundary_request` で返す。
    > 深さ1で数値を決めない原則（本文6の🧭）の例外だが、
    > **決めないと公平性の検査が原理的に書けない。**（2026-09-04）

## 6. 決めないこと・任せること

**深さ2または実装が決めてよい**

- 危険度の**コスト表と重み**（どの特性が何点か）
- 危険度予算の**具体的な値**と、ボスごとの配分
- 形状テンプレートを**何種類持つか**と、それぞれの当たり判定の形
- ~~プレイヤーの性能すべて（移動速度、ジャンプの高さと滞空、ローリングの無敵フレーム、
  パリィの受付、スタミナの最大値・消費量・回復速度と停止時間）~~
- **プレイヤーの性能のうち、公平性の検査に効かないもの** —
  スタミナの最大値・消費量・回復速度と停止時間、入力バッファ、加速度と摩擦、
  攻撃の威力とリーチ、キー割り当て

  > 🧭 **AI**: 2026-09-04 修正（Codex `duel-split-1` 前検査 **重大2・4**）。
  > **旧文は「プレイヤーの性能すべて」を委任していたが、そのうち
  > ジャンプの高さと滞空・ロールの無敵と移動距離・パリィ受付・歩行速度・当たり判定サイズは
  > 公平性の検査（本文4 の「公平性 B」）に直接効く。**委任したままだと、
  > `gen` は「ジャンプで越えられる」と宣言できるが**その真偽を確かめられない。**
  > この5つは本文5 の 12（プレイヤー能力契約）へ引き上げた。`provisional` なので、
  > 子は ±25% で調整できる。**スタミナは検査に効かないので委任のまま残した。**（2026-09-04）
- 攻撃後硬直・予備動作の**具体的なフレーム数**（下限 18F は根が置いた仮の前提）
- **山札の仕組み**（完全ランダムと固定順の中間をどう作るか）
- 体勢（第2の耐久値）の値と、崩れたときの隙の長さ
- **ボス何体で1ランか、1体あたりの目標時間**（根の 1.8 が仮置き。根の 6 から委任済み）
- 第2形態変異の中身
- 検査器の実装方法（区間演算か離散サンプリングか）

**今回は決めない**

- 敵の種類を増やす仕組み、ボス以外の敵、複数体同時
- プレイヤーの成長・装備・スキル選択

> 🧭 **AI**: 数値を1つも決めていないのは意図的である。
> **深さ1が決めるのは「課題の範囲と解決法」**であって数値ではない（`interview.md` の深さ表）。
> ここで固定すると、深さ2以降で容量に応じて削る判断ができなくなる。（2026-09-03）

## 7. 分からないこと・仮置きしていること

1. ~~山札に黄の技しか残っていない状況の扱い~~ → **解決済み**（2026-09-05）。
   **`fight.foe` の 1.2 が凍結した** — 未使用の残りが全部不適格なら**山札を復元して全技から選び**、
   それでも駄目なら 12F 待って再走査する。
   `gen` のセット条件（高速技は1体に1つまで）により、待機の段は到達不能。

   > ⚠️ **旧文は「仮置き: 山札を引き直す。`provisional: true`」だった。**
   > 2026-09-04 Codex 監査 **P0-2**（凍結した新ルールと旧仮置きの二重記述）の型なので、
   > 現行の命令として読めない形へ直した。（2026-09-05）
   **深さ2の「ボスの行動選択」で決める。**根へ返す必要は無いと判断した —
   これは山札の設計であって、根の決定（選択時条件そのもの）は変えないため

2. **検査器のコスト**が分からない。危険包絡の計算をフレームごとの離散サンプリングで行うと、
   10,000 シードの検証が実行時間の問題になる可能性がある。
   また実行時（ゲーム内）に走らせるなら、**生成の待ち時間**が発生する。
   仮置き: 生成はタイトル画面またはボス出現前の演出中に行い、体感を隠す

3. **補助条件「台本比較」の閾値 50% に根拠が無い。**台本入力が完全ランダムの半分以下の被弾、
   という線は直感で置いた。深さ2で実測して調整したくなる可能性がある。
   **ただし子は親の承認済み受け入れ条件を書き換えられない**ので、
   変えるなら `boundary_request` で `duel` を再開する（Codex 前検査 軽微3）。
   なお**根の条件（1回目と3回目の被弾比較）には閾値が無い**ので、この不確かさは
   補助条件の側だけに載っている

4. **容量 18,500 bytes に検査器が収まるか未検証。**生成器と検査器が二重に必要なので、
   `duel` の中で最も削られやすい。**削るとしたら検査の網羅度**（10,000 シード → 全数検査を
   実行時ではなくビルド時に回す等）だが、それは受け入れ条件「公平性」を弱める

5. **根の 1.8（ランの構造）は仮置きのまま。**体数が決まらないと総容量が見積もれない

## 分割提案 duel-split-1

status: **superseded**（2026-09-04。Codex の前検査で**判定不能**。重大8件・軽微5件。
提案側の照合でも13件とも成立したので `duel-split-2` へ差し替えた）
旧 status: pending（前検査待ち）
前提: `duel@2026-09-03-decision-1` — **失効**
（本文4・5・6 を修正したため。`review.md` §3。内容は `decision-2` に入っている）
前検査: **実施済み**（2026-09-04 / Codex）。結果は本節末尾

深さ1の子は**深さ2＝解決法**。`duel` の解決法（5層の骨格）を、
**時間軸で2つに切る。**

### 子

- `gen`: **戦闘が始まる前に確定するものすべて。**シードから技構成・数値・修飾子・外見・
  山札の中身を作り、**安全席の検査に通してから確定させる**。引き直しとフォールバックを含む。
  「生成物が公平であること」に責任を持つ
- `fight`: **戦闘が始まってから終わるまで。**入力・物理・当たり判定・スタミナ・体勢・
  攻撃後硬直・ボスの行動選択（選択時条件を含む）・ランの構造。
  「確定した敵と噛み合う1戦が成立すること」に責任を持つ

### seam（1 ID / 1端点・片方向）

- id: `d1.boss-definition`
  多重度: 1:1 / from: `gen` / to: `fight`
  内容: 1体のボスを戦わせるために必要な、確定済みの定義すべて
  - 技のリスト（各技: 形状ID / 数値 / 修飾子 / 特性ベクトル / 主色・副色 /
    **回答手段の宣言** / 床付近か高さありか）
  - **安全席テーブル**（技ごとの、安全な X 区間の集合）。
    `fight` はこれを**選択時条件の判定に使うだけ**で、再計算しない
  - 山札の中身（技の並びを決める材料）
  - 外見（体格・武器・主色・副色・緑の輪郭の有無）
  - ボスの HP・体勢の最大値・危険度の合計
  - 第2形態の変異（最終ボスのみ）
  - **シードと、生成に使った乱数系列の最終状態**（不変条件9の再現用）

**逆向きの seam は無い。**`fight` は `gen` へ何も返さない
（リトライ時は生成をやり直さず、同じ定義を使う。不変条件9）。
開く順序は `gen` → `fight` で循環しない。

### 親に残すもの

1. **根の不変条件12件**と、`duel` 自身の不変条件3件（本文5の 9〜11）。全子へ等しく継承する
2. **本文1の解決法5層**（予算 / 禁止組み合わせ / 安全席の検査 / 選択時の条件 / フォールバック）。
   子は詳細化するのであって選び直さない
3. **座標系と共有定数**（`duel` がここで決める。**seam にしない**）

   | 定数 | 扱い |
   | --- | --- |
   | 画面の内部解像度と戦闘領域の X 範囲 | `duel` が決める。両子が使う |
   | **プレイヤーの当たり判定サイズ（幅・高さ）** | `duel` が決める。`gen` は膨張の半幅に、`fight` は衝突に使う |
   | 座標系（X = 地上移動 / Y = ジャンプ中のみ / Z 無し） | 根の 1.7 から継承 |
   | フレームレート（60fps 固定） | `duel` が決める |

   > 🧭 **AI**: プレイヤーの当たり判定サイズを親が持つのが要点である。
   > **これを `fight` の持ち物にすると、`gen` の安全席計算が `fight` の決定に依存し、
   > `fight` → `gen` の逆向き seam が生まれて循環する。**
   > 根が知覚の下限を親に置いて `phase` 機構を消したのと同じ型。
   > 共有される定数は、seam ではなく親の決定にする。（2026-09-03）

4. **書き込み権限**: **形状定義の正本は `gen` が持つ**（根の親に残すもの3を降ろす）。
   `fight` は読むだけで、独自の当たり判定形状を持たない。
   `read` へ渡る `s1.presentation-state` の形状部分も、出所は `gen` の正本
5. **決定論の規約**: 乱数はシードから導出した1本の系列だけ。
   検査結果は浮動小数の丸めに依存しない形で判定する
6. **容量の目安**（`duel` の 18,500 inlined bytes を分ける。設計時見積もり）

   | 子 | 中身 | 目安 |
   | --- | --- | ---: |
   | `gen` | 生成器・危険度・検査器・フォールバック・外見 | **7,000** |
   | `fight` | 入力・物理・当たり判定・スタミナ・体勢・ボス行動・ラン構造 | **11,500** |

7. **`s1.presentation-state` の組み立て責任**: `read` へ渡す `s1` は
   **`fight` が組み立てる**（戦闘状態とライフサイクルを持つのが `fight` のため）。
   ただし形状と技の定義部分は `gen` の正本をそのまま載せる。**`fight` は形を作り直さない**
8. 予算: 深さ3・子数5

### 受け入れ条件の割り当て

| `duel` の受け入れ条件 | 担当 |
| --- | --- |
| 決定論 | `gen`（主）。`fight` はリトライで再生成しないことだけ守る |
| 公平性 | **`gen`**（安全席の検査そのもの） |
| 停止性 | **`gen`**（引き直し上限とフォールバック） |
| 利用可能性 | **`fight`**（選択時条件の実行）＋ `gen`（前提として安全席テーブルを渡す） |
| 学習が起きる | **`fight`**（主: 台本入力との比較は戦闘を回して測る）＋ `gen`（前提: 技が固定されること） |

未割り当ては無い。`deferred` も無い。

### 子ごとの振り分け（転記元・完全形）

```md
- child: gen
  責任: シードから、安全席の検査を通った公平なボス定義を、決定論的に作る
  詳細化の対象: 本文1の解決法5層のうち 1（危険度予算）2（禁止組み合わせ）
    3（安全席の検査）5（フォールバック）。根の 1.2（生成する/しないの境界）、
    1.3（危険度・特性と代償・危険包絡）、1.6（主色は最も危険度へ寄与した特性から決まる）
  継承する制約: 根の不変条件12件＋duel の不変条件3件（本文5の 9〜11）。とくに
    「公平性の保証は検査が持つ。予算と禁止列挙は最適化にすぎない」
    「生成は決定論的」「検査を通らない技は必ず捨てる」。
    形状定義の正本を持つ側としての書き込み権限（親に残すもの4）。
    座標系と共有定数（親に残すもの3。プレイヤーの当たり判定サイズを含む）。
    知覚の下限 18F / 同時2件は根の仮の前提であり、疑わずに使う。
    容量の目安 7,000 inlined bytes
  割り当てられた受け入れ条件: 決定論（主）、公平性、停止性、利用可能性（前提）
  uses_seams: []
  提供する seam: d1.boss-definition（fight へ）
  開く順序: 1番目
  parent_decision_ref: duel@2026-09-03-decision-1 ＋ duel@2026-09-03-split-1

- child: fight
  責任: 確定した敵と1人のプレイヤーが噛み合う1戦を成立させる
  詳細化の対象: 本文1の解決法5層のうち 4（選択時の条件）。根の 1.1（1戦の中身）、
    1.4（回答手段は最低2種類）、1.5（資源はスタミナ1本）、1.7 の Y 軸、1.8（ランの構造・仮置き）、
    1.10（技の奪取。所持・発動・撃破時の選択）
  継承する制約: 根の不変条件12件＋duel の不変条件3件。とくに 7（戦闘中の判定を生成しない）、
    10（反射神経に依存しない回答）。**当たり判定の形状を独自に持たない**
    （gen の正本を読むだけ・親に残すもの4）。
    座標系と共有定数（親に残すもの3）。回答手段は4つだけ、資源はスタミナ1本、空中制御なし。
    リトライ時に生成をやり直さない（不変条件9）。容量の目安 11,500 inlined bytes
  割り当てられた受け入れ条件: 利用可能性（主）、学習が起きる（主）、決定論（リトライの再現のみ）
  uses_seams: [d1.boss-definition]
  提供する seam: なし（ただし親経由で read へ渡る s1.presentation-state を組み立てる。
    親に残すもの7）
  開く順序: 2番目
  parent_decision_ref: duel@2026-09-03-decision-1 ＋ duel@2026-09-03-split-1
```

### 他の切り方との比較

- **5子案**（生成器 / 検査器 / 物理 / ボス行動 / ラン）: 却下。子数が予算上限で、
  **検査器と生成器の間に引き直しのループが通る**（検査結果が生成をやり直させる）。
  切ってはいけない場所を切っている
- **3子案**（`gen` / `fight` / `run`）: 却下。`run` を分けると
  `run → gen`（シードと予算）→ `fight` → `run`（結果）の**輪ができる**。
  ラン構造は根の 1.8 が仮置きで、削られる可能性もあるため `fight` の中に置く
- **敵側 / プレイヤー側で切る案**: 却下。根で既に却下した切り方と同じ理由
  （「形状 × 回答手段の宣言」が両者を縛る）
- **時間軸で切った理由**: 戦闘前に確定するものと戦闘中に動くものの境界は、
  **不変条件7（戦闘中の判定を生成しない）そのもの**である。
  この線で切ると、seam が「確定したボス定義」1本になり、逆向きが原理的に生まれない
- 子は2つ、深さ2。予算（深さ3・子数5）内

### この提案自体への疑い

1. **`fight` が大きい。**11,500 / 18,500 = 62%。入力・物理・当たり判定・スタミナ・体勢・
   ボス行動・ラン構造を1ノードで抱える。深さ3で4〜5子に割れなければ切り直しになる。
   **根の `duel` が大きいという指摘が、そのまま1段下へ移っただけ**とも読める
2. **安全席テーブルを `gen` が渡す設計は、容量を食う。**技ごとに X 区間の集合を持つので、
   実行時に再計算するほうが小さい可能性がある。ただし再計算させると
   `fight` が検査ロジックを持つことになり、**正本が2箇所になる**（不変条件8と同じ型の危険）
3. **ラン構造を `fight` に入れたが、性質が違う**（1戦の中と外）。
   根の 1.8 が仮置きのままなので保留にしたが、体数が確定したら切り直す可能性がある

### precheck（duel-split-1）

#### precheck 2026-09-04 by Codex

<!-- Codex（既定モデル / model_reasoning_effort: xhigh）。読ませたもの: harness 一式・
     precedents.md・祖先チェーン（root）・本ノード全文。兄弟（read）は読ませていない -->

重大:

1. **承認者が人間でない。**`precheck.md` §1「親ノードの内容に人間の決定の承認が無い場合も
   判定不能」と、本ノードの `duel@2026-09-03-decision-1`（承認者: 提案側 Claude / 種別: AI）が
   衝突する。`harness/README.md:120` と `roles/split.md:144` は承認者を人間に固定しており、
   事前委任を個別の人間承認とみなす規定は無い。`waiver-1` は**提案者自身への委任**であり、
   `precedents.md` 2026-08-31 の「AI が起草し人間が承認する」委任とは型が違う
2. **`親に残すもの3`（座標系と共有定数）が、承認済みの本文6と衝突する。**
   本文6は「プレイヤーの性能すべて」を深さ2以降へ委任しており、根の本文6も
   「画面の内部解像度、レイアウト」を下位へ委任している。分割提案で親へ引き上げているが、
   **値も契約も書かれていない**（`precheck.md` §1「項目名の列挙では足りない」）。
   内部解像度は本文1「含まないもの: 画面に何をどう出すか（`read` の担当）」とも境界が衝突する。
   **分割承認は決定承認を代用できない**
3. **ランの構造を `fight` に入れると、`fight` → `gen` の輪が残る。**
   根の 1.8 はミニラン（複数ボス）であり、`fight` が撃破後の進行を持つ以上、
   2体目以降で**ボス番号・次シード・危険度予算または生成要求**が `fight` から `gen` へ流れる。
   `d1.boss-definition` は「1体のボスを…」と1体分に限定されている。
   提案は3子案の却下理由で `run → gen → fight → run` の輪を自ら指摘しているが、
   **`run` を `fight` へ畳んでも `fight → gen → fight` は消えない**
4. **`gen` は「回答手段の宣言」の有効性を単独で判定できない。**
   移動速度・ジャンプの高さと滞空・ロールの無敵と移動量・パリィ受付・スタミナ制約は
   `fight` の持ち物（本文6）。根の共通語彙は「**宣言と実装が食い違ってはならない**」とし、
   根の 1.4 は「最低2種類の有効な対処」を要求する。`親に残すもの3` が共有しているのは
   当たり判定サイズだけで、**ジャンプで越えられるかを `gen` が判定する材料が無い。**
   不整合が判明したときの差し戻し経路も無く、兄弟不可視性が成立しない
5. **`d1` は「形状ID」しか渡さないのに、`fight` が当たり判定を行う。**
   `親に残すもの4` は形状定義の正本を `gen` に置く。兄弟を読めない `fight` は、
   ID だけでは衝突判定も、根の `s1.presentation-state` が要求する危険範囲座標も作れない。
   さらに `親に残すもの7` は `fight` が `s1` を組み立てるとするのに、
   `fight` の振り分けは「**提供する seam: なし**」と書いている。
   隠れた `gen → fight` / `gen → read` 経路が残る
6. **受け入れ条件「公平性」が、親の決定の範囲を変更している。**
   根の 1.3 は安全席の検査を**黄の技に限定**し、「移動して避けるしかない技は黄にできない」と
   明記している。本文4は「**生成されたすべての技が**、安全席の検査を通っている」とし、
   非黄技にまで静止可能な安全席を要求している。これは詳細化ではなく範囲変更である。
   また**安全席は回答を1つしか証明せず**、根の 1.4 の「最低2種類」を検査していない
7. **受け入れ条件「学習」が代理条件に差し替わっている。**
   根の判定方法は「同一シードへの連続挑戦を記録し、1回目と3回目の被弾数を比べる」。
   本文4は「ランダム入力 vs 台本入力」で、証明できるのは
   **「知識を持つ入力が有利になり得ること」だけ**であり、学習が起きることではない。
   根の `親に残すもの6`「子は条件そのものを書き換えない」に反する
8. **`duel-split-1` の承認が、根の `deferred` の再開条件そのものである。**
   根の `親に残すもの11`「実装基盤の裁定 — `deferred`。再開条件: **最初の深さ2の分割承認まで**」。
   提案は承認後そのまま `gen` / `fight` を開くとしており、
   **root を再開して基盤を決めるゲートが無い。**root の再承認と、それに伴う
   `duel` の決定・分割の失効範囲を先に定める必要がある

軽微:

1. `fight` の責任は「戦闘が始まってから終わるまで」だが、詳細化の対象に
   1.8（ランの構造＝戦闘間の回復・次ボス遷移）が入っている。責任文と対象が一致していない
2. 「公平性」は「すべての技」、「停止性」は「どのシードでも」と**全称**で書かれているが、
   判定方法は 10,000 シードの標本検査だけ。構造上の証明か、全入力を覆う有限集合の定義が要る
3. 本文7-3 は学習判定の 50% 閾値を「深さ2で実測して調整する可能性」とするが、
   子は親の承認済み受け入れ条件を書き換えられない。`boundary_request` で `duel` を
   再開すると明記すべき
4. 根の消し残し2件。`index.md:1959` の `split-6` 冒頭が `status: pending` のまま
   （有効状態表は `active`）。`index.md:2408` の `decision-5` が `status: active` のまま
   （`decision-6` の記述は「`decision-5` は失効」）
5. 7,000 ＋ 11,500 で 18,500 を**全量配分**しており、共有定数・`d1` の変換・`s1` の組み立てに
   余白が無い。提案自身が認める `fight` の肥大化リスクを吸収できない

確認項目別の結果:

| 項目 | 結果 |
| --- | --- |
| 1. 提案の必須項目 | **判定不能。**各節はあるが、人間による親決定の承認が無く、`親に残すもの3` も未決定の項目名に留まる |
| 2. 深さの整合性 | **要修正。**`gen` / `fight` は深さ2として読めるが、`fight` の責任文とラン構造が不一致 |
| 3. seam の数え落とし | **不適合。**次ボス生成・能力契約・不整合の差し戻し・形状正本・`s1` の経路が `d1` 1本に閉じていない |
| 4. 受け入れ条件の割り当て | **不適合。**最低2回答と実際の学習が未検査。共同担当に統合失敗時の所有者が無い |
| 5. 兄弟の不可視性 | **不適合。**`gen` は `fight` の能力仕様を要し、`fight` は `d1` に無い形状正本を要する |
| 6. 自己適用 | **不適合。**3子案で認識した輪を `fight` へ畳んで消えたと扱い、親へ移した共有定数を未決定のまま seam 数から除外している |
| 7. 文書間の矛盾 | **不適合。**承認者規約・安全席検査の対象・学習の判定方法・実装基盤の再開条件・有効状態表 |
| 8. 予算 | **適合。**子2つ・深さ2で上限内。ただしバイト見積もりに余白が無い |

**判定: 判定不能**

過去判断との関係（Codex 記）:

- 2026-08-25「幸せな道だけを数えて逆向き・例外・失効経路を落とす」、2026-09-01「seam は ID では
  なく端点で数える」を、**次ボス生成と不整合の差し戻し**へそのまま適用した。覆していない
- 2026-09-01「**親へ移す**を**結合が消える**と取り違えた」と**同型**である。当たり判定サイズを
  親の項目名へ移しても、回答手段の有効性に必要な `fight` 側の能力仕様までは消えていない
- 2026-09-01「担当がいるように見せかけた割り当て」と**同型**である。「公平性」「学習」に子名は
  付いているが、最低2回答と人間の反復学習を判定する入力・実行者・統合責任が不足している
- 2026-08-31「敵側／プレイヤー側で切らず `duel` にまとめた」の根拠だった
  「**形状 × 回答手段の宣言**」の結合が、今回 `gen` と `fight` の間で再び現れている。
  時間軸で切る判断自体を否定するものではないが、その結合を親契約か seam として明示する必要がある
- 2026-08-25「承認者は人間に固定」を維持する。2026-08-31 の AI 起草委任は**人間が最終承認する**
  前例であり、提案者自身への承認委任を認める前例ではない

#### 提案側（Claude）による照合 2026-09-04

指摘13件をすべて実物と照合した。**13件とも成立する。**

| 指摘 | 照合した実物 | 判定 |
| --- | --- | --- |
| 重大1 | `harness/README.md:120` / `roles/split.md:144` / 本ノード承認証跡 | 成立（規約上）。ただし `waiver-1` は人間の明示の発話であり、**解除できるのは人間だけ** |
| 重大2 | 本文6「プレイヤーの性能すべて」／本文1「含まないもの: 画面に何をどう出すか」／根 6 | **成立。**`split-6` の前検査 重大1（分割承認が決定承認を代用）と**同じ型の再発** |
| 重大3 | 根 1.8／`fight` の詳細化対象に 1.8／`d1` の「1体のボスを」／「他の切り方との比較」の3子案 | **成立。最も重い。**自分で書いた却下理由が自分に当たっている |
| 重大4 | 根の共通語彙「宣言と実装が食い違ってはならない」／根 1.4／本文6／`親に残すもの3` | **成立。**「共有定数は親が持つ」で消したつもりの逆向き依存が、当たり判定サイズ**以外**に残っていた |
| 重大5 | `d1` の技リスト（形状ID のみ）／`親に残すもの4`・`7`／`fight` の「提供する seam: なし」 | 成立 |
| 重大6 | 根 1.3「**黄の技は**、生成後に次を検査する」／根 1.3 の🧭「移動して避けるしかない技は黄にできない」／本文4「生成された**すべての技**が」 | **成立。**これは手続きではなく**ゲーム設計の実害**。全技に静止安全席を要求すると、移動して避ける技が全部生成できなくなる |
| 重大7 | 根 4「判定: 同一シードへの連続挑戦を記録し、1回目と3回目の被弾数を比べる」／根 `親に残すもの6`／本文4 | 成立 |
| 重大8 | 根 `親に残すもの11`「再開条件: 最初の深さ2の分割承認まで」 | **成立。**承認した瞬間に発動する条件を、提案が扱っていない |
| 軽微1〜3 | 各該当箇所 | 成立 |
| 軽微4 | `index.md:1959`（`pending` のまま）／`index.md:2408`（`active` のまま） | 成立。根の消し残し |
| 軽微5 | `親に残すもの6` の配分表 | 成立 |

**まとめ**（提案側の自己評価）:

- **重大3・4 が同じ出どころ**である。「共有される定数は seam ではなく親の決定にする」という
  根で効いた型を `duel` へ適用したが、**適用が浅かった。**当たり判定サイズは移したが、
  ジャンプの高さ・滞空・ロールの無敵といった「回答手段の宣言を検証するのに必要な値」は
  `fight` に残したまま、`gen` に宣言を作らせている。ラン構造も同じで、
  「戦闘の外」を `fight` に畳んだだけで、**畳んだ先から親へ出る線が残った**
- **重大6 だけは分割の問題ではなく、決定本文（`decision-1`）の問題**である。
  修正すると `duel@2026-09-03-decision-1` の承認は失効する（`review.md` §3）
- **重大8 は手順の問題**で、`duel-split-1` を承認する前に root を再開する必要がある

## 分割提案 duel-split-5

status: **approved**（`duel@2026-09-05-split-5` / 人間の承認 `root@2026-09-05-approval-2`。`children-created`）
前提: **`duel@2026-09-05-decision-8`**（本文1〜7。`decision-1` は本文4・5・6 の修正により失効）
前検査: **未実施のまま承認した。**Codex がリソース上限で回せない。
理由は承認証跡の `split-2` の項に記録した（`review.md` の要求）
`duel-split-1` との関係: **superseded。**子の切り口（時間軸）と子の名前は変えていない。
**変えたのは、輪と隠れた依存を消すための4点**である。

### `duel-split-1` から何を変えたか

| # | 変更 | 消えた指摘 |
| --- | --- | --- |
| 1 | **`gen` はラン全体を一度に生成する。**「ボス1体」ではなく「1ランぶんの全ボス」を作る | 重大3（`fight → gen` の輪） |
| 2 | **プレイヤー能力契約を `duel` が持つ**（本文5 の 12。値まで記載・`provisional`） | 重大2・4 |
| 3 | **形状の意味論を `duel` の共通語彙へ置き、`d1` は展開に必要な全パラメータを載せる** | 重大5 |
| 4 | **`fight` が `s1.presentation-state` の提供者であると明記する** | 重大5 の後半 |

**足したのではなく、置き場所を変えただけである。**新しい機構（状態機械・報告経路・
再提案プロトコル）は1つも増えていない。根が `split-5` → `split-6` でやったのと同じ方向。

#### 変更1 がなぜ輪を消すか

`duel-split-1` は `fight` に「ランの構造」を持たせた。ランは複数ボスなので、
2体目へ進むときに `fight` が `gen` へ「次を作れ」と言う必要があり、
**`fight → gen` の逆向きが生まれていた。**

`gen` が**ラン開始時に全ボスを作り切る**と、この線が消える。

| | `duel-split-1` | `duel-split-2` |
| --- | --- | --- |
| `gen` の出力単位 | ボス1体 | **1ランぶん全部** |
| `fight` → `gen` | **必要**（次のボス） | **不要** |
| 不変条件9 との関係 | シード → 1体 | **シード → ラン全体**。より強い再現性 |

**根の 1.8 とも整合する** — 「難易度は敵側の危険度予算だけで上げる（プレイヤー側の成長で
上げない）」ので、**N 体目の予算はシードとボス番号だけで決まり、戦績に依存しない。**
`fight` の結果を待つ理由が、そもそも無かった。

> 🧭 **AI**: Codex が示した処方をそのまま採った（「全ランを開始前に生成する案なら、
> `d1` の『1体』と子責任をその形へ変更する必要がある」）。
> **提案側の創作ではないぶん、外し方が小さい。**
> 代償は容量で、全ボスの定義を同時に持つことになる。3〜5体なら誤差だが、
> 体数が増えたら**安全席テーブルを持ち回るのをやめて実行時計算へ倒す**判断が要る
> （本文7 の 4 と同じトレードオフ）。（2026-09-04）

### 子

- `gen`: **1戦でも1ランでも「始まる前に確定するもの」すべて。**
  シードから**ラン全体**（全ボスの技構成・数値・修飾子・外見・山札の中身・出現順と予算配分）を
  作り、**公平性の検査2つ（安全席／回答2つ）に通してから確定させる。**
  引き直しとフォールバックを含む。「**生成物が公平であること**」に責任を持つ
- `fight`: **確定したランを最初から最後まで走らせる。**入力・物理・当たり判定・スタミナ・
  体勢・攻撃後硬直・ボスの行動選択（選択時条件を含む）・**戦闘間の遷移と回復**。
  「**確定した敵と噛み合う1戦が成立し、それがランとして続くこと**」に責任を持つ

> 🧭 **AI**: `fight` の責任文から「戦闘が始まってから終わるまで」を外した
> （Codex 前検査 **軽微1**）。ラン構造（戦闘間の回復・次ボスへの遷移）を持たせながら
> 「1戦の中」と書いていて、責任文と詳細化対象が食い違っていた。（2026-09-04）

### seam（1 ID / 1端点・片方向）

- id: `d1.run-definition`
  多重度: 1:1 / from: `gen` / to: `fight`
  内容: **1ランを最後まで走らせるために必要な、確定済みの定義すべて。**
  <!-- 旧 d1.boss-definition。1体分から1ラン分へ変えたので ID も変えた -->
  - **ボスの配列**（出現順。各要素が1体ぶんの定義）
  - 各ボス:
    - 技のリスト。各技は **`fight` が当たり判定を再現できる全パラメータ**を持つ —
      形状種別 / 寸法 / ボス原点からのオフセット / 各段の発生・持続フレーム /
      ボスの移動と追尾のパラメータ / 特性ベクトル / 主色・副色 /
      **有効な回答手段の集合**（`gen` が本文5 の 12 に対して検査した結果）/ 床付近か高さありか
    - **安全席テーブル**（黄の技ごとの、安全な X 区間の集合）。
      **区間は「ボス中心 X からの相対オフセット」と「ボスの向き」の組で持つ。**
      `fight` は**選択時条件の判定に使うだけ**で、再計算しない

      > 🔒 **凍結（`duel@2026-09-05-decision-8`）。**
      > 2026-09-05 の再監査（**重大5**）:
      > **「`d1` の安全席が絶対 X 区間なのに、基準となるボス位置・向きが無い。**
      > CHARGE などの包絡は現在のボス X と向きで変わる。
      > `foe` は再計算禁止なので、**生成時の絶対区間を戦闘中の任意のボス位置へ
      > どう適用するか定義不能。」**
      >
      > **相対で持つ。**`foe` は選択の瞬間に
      > `安全席の絶対区間 = ボス中心X + 向き × オフセット区間` を計算し、
      > **プレイヤー中心がそこに入っているかだけを見る。**
      > **これは「再計算」ではない** — 包絡も安全席も作り直さず、**平行移動と反転だけ**である。
      >
      > **戦闘領域の端で切る**（X ∈ [20, 602]）。切った結果が
      > プレイヤー1人分の幅を割ったら、**その技はその位置では不適格**として扱う
    - **有効な回答手段の集合**（`gen.audit` が能力契約に対して検査した結果。
      移動 / ジャンプ / ロール / パリィ のうちどれが実際に効くか。
      **`m[F]` のフラグではなく、検査済みの集合である**）
    - **予備枠のフォールバック技**（全ボスが必ず1つ持つ。`roster` 1.2.1 の固定値）。
      **山札に入らず、`fight.foe` の段5 でだけ使う。**技数の検査に数えない
    - 山札の中身 / 外見 / HP / 体勢の最大値 / 危険度の合計
    - **第2形態（最終ボスのみ）: 変異の記述ではなく、変異後の技リストそのもの。**
      安全席テーブルと有効回答集合も**変異後の版を持つ**
  - **シードと、生成に使った乱数系列の最終状態**（不変条件9 の再現用）

  > 🔒 **凍結（`duel@2026-09-04-decision-3` / `split-3`）。**
  > 2026-09-04 の Codex 監査で2点が指摘された。
  >
  > **着手前6**: 旧 `d1` は**有効回答集合を持たなかった**ので、
  > `s1` が要求する「有効な回答手段の集合」の**正本が作れなかった。**追加した。
  >
  > **着手前5**: 旧 `d1` は**第2形態の「変異の記述」しか運ばず**、
  > `gen` に「変異後を再検査する」契約が無かった。
  > **`fight.foe` が検査済みデータを実戦側で書き換える**ことになり、
  > **リトライ等では消せた `fight → gen` の逆流が、第2形態だけ残っていた。**
  > **`gen` が両形態を確定・検査し、`fight` は切り替えるだけ**にした。
  > 変異は決定論的（固有技の威力・予備動作・硬直を +1）なので、生成時に両方作れる。

**形状種別の意味論（種別 → X×Y の矩形または円への展開規則）は `duel` が持つ**
（親に残すもの4）。`fight` は `d1` のパラメータと親の展開規則だけで判定を再現でき、
**`gen` の設計書を読む必要が無い。**

**逆向きの seam は無い。**`fight` は `gen` へ何も返さない。
リトライでも次のボスでも生成をやり直さず、同じ定義を使う（不変条件9）。
開く順序は `gen` → `fight` で循環しない。

> 🧭 **AI**: `duel-split-1` の `d1` は「形状ID」しか渡していなかった
> （Codex 前検査 **重大5**）。ID だけでは `fight` は当たり判定を作れず、
> 兄弟の設計書を読むしかなくなる。**「正本は `gen` が持つ」と書いただけで、
> 正本の中身を運ぶ経路を用意していなかった。**（2026-09-04）

### 親に残すもの

1. **根の不変条件12件**と、`duel` 自身の不変条件4件（本文5 の 9〜12）。全子へ等しく継承する
2. **本文1の解決法5層**（予算 / 禁止組み合わせ / **3a 安全席の検査** / **3b 回答2つの検査** /
   選択時の条件 / フォールバック）。子は詳細化するのであって選び直さない
3. **プレイヤー能力契約と座標系**（本文5 の 12。**値まで書いてある**・`provisional: true`）。
   当たり判定の半幅・高さ、ジャンプの最高到達高さと滞空、ローリングの無敵と総フレームと移動距離、
   パリィ受付、歩行速度、内部解像度と戦闘領域の X 範囲、60fps 固定。
   **子は ±25% で調整してよい。範囲外は `boundary_request` で `duel` へ返す**

   > 🧭 **AI**: `duel-split-1` はここを「`duel` が決める」と書きながら**値を書いていなかった**
   > （Codex **重大2**）。`precheck.md` §1 は「親に残すものは、子が独立に決められる程度に
   > 内容が書かれているか」を見ると定める。項目名だけでは `gen` が検査器を書けない。
   >
   > さらに**当たり判定サイズしか移していなかった**（**重大4**）。
   > 「ジャンプで越えられる」という宣言の真偽は、ジャンプの高さが決まらないと判定できない。
   > `fight` に残したままだと `fight → gen` の逆向きが残る。（2026-09-04）

4. **形状の意味論と書き込み権限**

   > 🔒 **凍結（`duel@2026-09-04-decision-3`）。展開規則の実体はここにある。**
   > 2026-09-04 の Codex 監査（**着手前2**）で、
   > **旧文は「`duel` が持つ」と宣言しただけで、表は `arena` にしか無かった**ことが指摘された。
   > 兄弟不可視の `gen.audit` が参照できず、**安全性検査と実戦の当たり判定が別実装になる。**
   > **前回の重大5 が閉じていなかった。**表をここへ移した。

   **形状種別 → 危険範囲への展開規則**（`射程` は `REACH[m[R]-1]`、`向き` は ±1）

   | 形状 | 展開結果 | ジャンプで越えられるか |
   | --- | --- | --- |
   | 0 **SWEEP** | 矩形。`x = 向き<0 ? ボスX−射程 : ボスX+40` / `y = 床−24` / `w = 射程` / `h = 24` | **越えられる**（床上 24px） |
   | 1 **THRUST** | 矩形。`x = 向き<0 ? ボスX−射程 : ボスX+38` / `y = ボスY+26` / `w = 射程` / `h = 18` | 越えられない |
   | 2 **SLAM** | 矩形。`x = ボスX−射程×0.55` / `y = 床−57` / `w = 射程+44` / `h = 57` | 越えられない |
   | 3 **CHARGE** | ボス本体の矩形（37 × 68）。**`active` 中はボスが前進する**（`3.2 + 射程段階×0.72` px/F） | **越えられる** |
   | 4 **SHOT** | **矩形を持たない。**弾として処理する（完全な仕様は下の 4.1） | 越えられない |
   | 5 **RAIN** | 矩形。`x = 標的X − w/2` / `y = 18` / `w = 28+射程段階×9` / `h = 床−18` | 越えられない |

   - **多段は同じ規則を段ごとに適用する。**`RAIN` だけ段ごとに標的 X が違う
     （`clamp(標的X ± (55 + 18×段), 30, 幅−30)`）
   - **追尾は予備動作の前半 62% までだけ標的を更新する。**`active` 以降は動かない（不変条件7）

   #### 4.1 SHOT の完全な時空間仕様

   > 🔒 **凍結（`duel@2026-09-05-decision-8`）。**
   > 2026-09-04 Codex 監査 **P0-5**: 「展開規則は SHOT について速度と Y 追尾に触れるだけで、
   > **発射位置・弾の当たり判定寸法・寿命・追尾更新式・段ごとの発射時刻を定義していない。**
   > 一方 `audit B` は全初期位置・全段・弾道を含む保守的到達可能性を要求する。
   > **この入力不足では SHOT の有効回答集合を判定できない。」**
   >
   > **`audit` と `arena` はこの表の同じ純関数を使う。**別実装にしない。

   | 項目 | 値 | 出所 |
   | --- | --- | --- |
   | **発射位置** | `x = ボスX + 20 + 向き × 22` / `y = ボスY + 29` | 実装 `spawnBossShot` |
   | **発射時刻** | 各段の開始フレーム。段間隔は `持続 + 10F`。**段ごとに1発** | 同 `activeBossMove` |
   | **速度（X）** | `向き × (3.1 + (5 − 予備動作段階) × 0.45)` px/F。**予備動作が短いほど速い** | 同 |
   | **速度（Y）** | `(狙いY − 発射Y) / max(60, |ボス中心X − プレイヤー中心X|) × 速度X の絶対値` | 同 |
   | **狙い Y** | 予備動作開始時に固定（`プレイヤーY + 14`）。追尾なら前半62%まで更新 | 同 `startMove` |
   | **追尾の更新式** | 毎フレーム `vy += sign(プレイヤーY + 14 − 弾Y) × 追尾段階 × 0.018`、**`vy` は ±1.3 に制限** | 同 `shotsStep` |
   | **当たり判定** | **10 × 10 px**（弾中心の矩形） | 同 |
   | **寿命** | **220 F**、または画面外（X が −30〜幅+30 の外 / Y が −30〜高さ+30 の外）で消滅 | 同 |
   | **パリィ** | 可能。成立すると**所有者が反転**し、速度 X が ×(−1.35)、速度 Y が ×(−1) になる | 同 |

   > 🔒 **SHOT は `m[F]` に `PARRY` を立てる**（`move` 1.6）。
   > 2026-09-05 の再監査（**重大4**）で、3者が食い違っていた —
   > 共通仕様は「パリィ可能」、`move` の宣言は「移動」だけ、
   > `audit`/`arena` は `m[F] & PARRY` が立つ技だけをパリィ回答として扱う。
   > **`audit` と `arena` が同じ SHOT を別の回答集合として扱う**状態だった。
   >
   > **実装は `m[F]` とは別に弾へ `parry: 1` を持たせている**が、
   > **ツリーは経路を1本にする** — 判定はすべて `m[F] & PARRY` を見る。

   **`audit` の危険包絡は、この弾道を発射から寿命または画面外までたどって求める。**
   追尾の更新式は**プレイヤー位置に依存する**ので、
   **包絡はプレイヤーが取りうる全 Y 位置に対する到達域の和集合**として計算する
   （保守的な側）。

   > ⚠️ **追尾付き SHOT は、この定義により黄を持てない**（根の 1.3）。
   > 危険形状の変換にプレイヤー状態を使うため。**`audit` の A の除外条件がここで効く。**

   **この表を使うのは `gen.audit`（危険包絡の計算）と `fight.arena`（当たり判定）の2箇所で、
   両方とも同じ規則を読む。**どちらも独自の展開を持たない。

   - **各技のパラメータの正本は `gen` が持つ**（根の親に残すもの3を降ろす）
   - **各技のパラメータの正本は `gen` が持つ**（根の親に残すもの3を降ろす）
   - **`fight` は独自の当たり判定形状を持たない。**`d1` のパラメータを親の規則で展開するだけ
   - `read` へ渡る `s1.presentation-state` の形状部分も、出所は `gen` の正本
5. **決定論の規約**: 乱数はシードから導出した1本の系列だけ。
   検査結果は浮動小数の丸めに依存しない形で判定する。
   **ラン全体が1つのシードから決まる**（不変条件9）
6. **`s1.presentation-state` の提供者は `fight` である。**
   `fight` が組み立てて `duel` の `seam_inbox` へ出し、`duel` が権威化して `read` へ降ろす。
   戦闘状態とライフサイクルを持つのが `fight` のため。
   ただし**技と形状の定義部分は `gen` の正本をそのまま載せ、`fight` は形を作り直さない**

   > 🧭 **AI**: `duel-split-1` はここを親に書きながら、
   > `fight` の振り分けには「**提供する seam: なし**」と書いていた（Codex **重大5** の後半）。
   > 実際には提供者なので、振り分け側にも書いた。（2026-09-04）

7. **容量の目安**（`duel` の 18,500 inlined bytes を分ける。設計時見積もり）

   | 取り分 | 中身 | 目安 |
   | --- | --- | ---: |
   | `gen` | 生成器・危険度・検査器2つ・フォールバック・外見 | **6,800** |
   | `fight` | 入力・物理・当たり判定・スタミナ・体勢・ボス行動・ラン進行 | **10,900** |
   | **親（`duel`）** | 能力契約の定数表・形状の展開規則・`s1` の組み立て | **800** |
   | | **合計** | **18,500** |

   > 🧭 **AI**: `duel-split-1` は 7,000 ＋ 11,500 で**全量を子へ配り切っていた**
   > （Codex **軽微5**）。親が持つ共有定数と展開規則の置き場が無かった。（2026-09-04）

8. 予算: 深さ3・子数5
9. **実装基盤は `prismatic-duel/`**（根の本文 6.1）。
   **根の `deferred` はこの分割の承認条件だったが、2026-09-04 に解除済み**なので、
   この提案の承認は根の再開を要さない

   > 🧭 **AI**: `duel-split-1` は、**自分の承認が根の `deferred` の再開条件そのもの**である
   > ことを扱っていなかった（Codex **重大8**）。
   > `duel-split-2` を出す前に根を再開し、`decision-7` / `split-7` で閉じた。（2026-09-04）

### 受け入れ条件の割り当て

| `duel` の受け入れ条件 | 担当 |
| --- | --- |
| 決定論 | **`gen`**（主）。`fight` はリトライ・次ボスで再生成しないことだけ守る |
| **公平性 A: 安全席** | **`gen`**（黄の技の検査そのもの） |
| **公平性 B: 回答が2つ** | **`gen`**（親の能力契約に対して検査する。`fight` の決定を待たない） |
| 停止性 | **`gen`**（引き直し上限とフォールバック） |
| 利用可能性 | **`fight`**（選択時条件の実行）＋ `gen`（前提として安全席テーブルを渡す） |
| 学習が起きる（根の判定） | **`fight`**（主: 同一シードへの連続挑戦を走らせる）＋ `gen`（前提: 技が固定されること） |
| 学習が起きる（補助・台本比較） | **`fight`**（主） |

未割り当ては無い。`deferred` も無い。

**2子にまたがる条件の統合責任**: `利用可能性` と `学習が起きる` は
**主担当が判定の責任を持ち、不成立だった場合に原因が前提側にあると判断したら
`boundary_request` で `duel` へ返す。**主担当が勝手に前提側の設計を変えない。

> 🧭 **AI**: `duel-split-1` は「未割り当ては無い」と書いただけで、
> **2子にまたがったときに誰が負けを引き受けるかを書いていなかった**
> （Codex **重大6・7** と、`split-1` 重大4 の「担当がいるように見せかけた割り当て」の型）。
> **公平性 B を `gen` 単独にできたのは、能力契約を親へ上げたからである** —
> `gen` は `fight` を待たずに検査できる。（2026-09-04）

### 子ごとの振り分け（転記元・完全形）

```md
- child: gen
  責任: シードから、公平性の検査2つを通ったランまるごとのボス定義を、決定論的に作る
  詳細化の対象: 本文1の解決法5層のうち 1（危険度予算）2（禁止組み合わせ）
    3a（安全席の検査）3b（回答2つの検査）5（フォールバック）。根の 1.2（生成する/しないの境界）、
    1.3（危険度・特性と代償・危険包絡）、1.6（主色は最も危険度へ寄与した特性から決まる）、
    1.8 のうち「ボス何体で1ランか・出現順・体ごとの危険度予算」
  継承する制約: 根の不変条件12件＋duel の不変条件4件（本文5の 9〜12）。とくに
    「公平性の保証は検査が持つ。予算と禁止列挙は最適化にすぎない」
    「生成は決定論的」「検査を通らない技は必ず捨てる」。
    プレイヤー能力契約（本文5の12・親に残すもの3。値まで記載・provisional・±25%まで調整可）。
    形状の展開規則は duel が定義し、各技のパラメータの正本を gen が持つ（親に残すもの4）。
    ラン全体を1つのシードから決める（親に残すもの5）。
    知覚の下限 18F / 同時2件は根が置いた仮の前提であり、疑わずに使う。
    実装基盤は prismatic-duel/（コードと build 環境のみ）。
    容量の目安 6,800 inlined bytes
  割り当てられた受け入れ条件: 決定論（主）、公平性A（主）、公平性B（主）、停止性（主）、
    利用可能性（前提）、学習が起きる（前提）
  uses_seams: []
  提供する seam: d1.run-definition（fight へ）
  開く順序: 1番目
  parent_decision_ref: duel@2026-09-05-decision-8 ＋ duel@2026-09-05-split-5

- child: fight
  責任: 確定したランを、1戦ごとに噛み合う戦闘として最後まで走らせる
  詳細化の対象: 本文1の解決法5層のうち 4（選択時の条件）。根の 1.1（1戦の中身）、
    1.4（回答手段は最低2種類）、1.5（資源はスタミナ1本）、1.7 の Y 軸、
    1.8 のうち「1体ごとの回復と次ボスへの遷移・1体あたりの目標時間」
  継承する制約: 根の不変条件13件＋duel の不変条件5件。とくに 7（戦闘中の判定を生成しない）、
    10（反射神経に依存しない回答）、13（奪った技はプレイヤーを強くしない）。
    奪取技の変換規則とスタミナ式（duel の本文5 の 13）。
    **当たり判定の形状を独自に持たない**
    （d1 のパラメータを親の展開規則で展開するだけ・親に残すもの4）。
    プレイヤー能力契約（本文5の12・親に残すもの3。±25%まで調整可、範囲外は boundary_request）。
    回答手段は4つだけ、資源はスタミナ1本、空中制御なし。
    リトライでも次ボスでも生成をやり直さない（不変条件9）。
    実装基盤は prismatic-duel/（コードと build 環境のみ）。
    容量の目安 10,900 inlined bytes
  割り当てられた受け入れ条件: 利用可能性（主）、学習が起きる（主・根の判定と補助の両方）、
    決定論（リトライと次ボスで再生成しないことのみ）
  uses_seams: [d1.run-definition]
  提供する seam: s1.presentation-state（duel の seam_inbox 経由で read へ。親に残すもの6）。
    技と形状の定義部分は gen の正本をそのまま載せ、形を作り直さない
  開く順序: 2番目
  parent_decision_ref: duel@2026-09-05-decision-8 ＋ duel@2026-09-05-split-5
```

### 他の切り方との比較

- **`duel-split-1`（`gen` が1体ずつ作る案）**: 却下。`fight` がラン構造を持つ以上、
  次ボスの生成要求が `fight → gen` へ流れて**輪ができる**（Codex 重大3）。
  切り口ではなく**生成の単位**が間違っていた
- **5子案**（生成器 / 検査器 / 物理 / ボス行動 / ラン）: 却下。子数が予算上限で、
  **検査器と生成器の間に引き直しのループが通る**（検査結果が生成をやり直させる）。
  切ってはいけない場所を切っている
- **3子案**（`gen` / `fight` / `run`）: 却下。`run` を分けると
  `run → gen`（シードと予算）→ `fight` → `run`（結果）の**輪ができる**。
  変更1（`gen` がラン全体を作る）を入れると `run` の仕事は
  「回復して次へ進む」だけになり、独立したノードにする量が無い
- **敵側 / プレイヤー側で切る案**: 却下。根で既に却下した切り方と同じ理由
  （「形状 × 回答手段の宣言」が両者を縛る）。
  **なお Codex は、この結合が `gen` と `fight` の間で再び現れていると指摘した。**
  変更2（能力契約を親が持つ）で、結合を**親の決定として明示**した
- **時間軸で切った理由**: 確定するものと動くものの境界は、
  **不変条件7（戦闘中の判定を生成しない）そのもの**である。
  この線で切ると seam が「確定したラン定義」1本になり、逆向きが原理的に生まれない
- 子は2つ、深さ2。予算（深さ3・子数5）内

### この提案自体への疑い

1. **`fight` が大きい。**10,900 / 18,500 = 59%。`duel-split-1` の 62% から少し減っただけで、
   **根の「`duel` が大きい」という指摘が1段下へ移っただけ**という読みは依然できる。
   深さ3で4〜5子に割れなければ切り直しになる
2. **安全席テーブルをラン全体ぶん持ち回るのは、容量を食う。**
   `duel-split-1` の「1体ぶん」より増えている。実行時に再計算するほうが小さいが、
   再計算させると `fight` が検査ロジックを持ち、**正本が2箇所になる**
   （不変条件8 と同じ型の危険）。体数が確定したら測り直す
3. **プレイヤー能力契約の値に根拠が無い。**480×270 の内部解像度に対する比率として
   直感で置いた。`prismatic-duel/game.js` の実測値と突き合わせていない。
   `provisional` かつ ±25% の調整幅を認めているのはこのためだが、
   **±25% で足りるかも検証していない**
4. **`gen` がラン全体を作ると、生成の待ち時間が1回に集中する。**
   本文7 の 2（検査器のコスト）がそのまま効く。タイトル画面で隠す前提だが、
   体数 × 技数 × 引き直し8回の最悪ケースを見積もっていない

### precheck（duel-split-2）

#### precheck 2026-09-04（深さ3全体）by Codex

<!-- 対象は duel-split-2 単体ではなく、深さ3まで書き下ろされたツリー全体（20ノード）。
     Codex は外部監査者なので兄弟不可視の制約を受けない（根の split-7 の論証。
     Codex 自身も「その論証は妥当」と判定した）。
     読ませたもの: harness 一式・precedents・AS_BUILT・ツリー全ノード・prismatic-duel のコード -->

**総括（Codex 記）**: 全面的な作り直しは不要。ただし現在のまま実装すると
**安全性判定・第2形態・表示用状態のいずれかが誤る**ので、
**親契約を最小限修正してから着手すべき。**

**■ 実装を止める / 間違ったものが出来る（着手前に直すべき）— 9件**

1. **`s1` が表示側の必須データを運べない。**
   根の `s1` 定義（`split-7` の seam 節）は技座標・残り時間・HP 等だけ。しかし
   `actors` はプレイヤー座標・行動・無敵、ボス座標・向き・外見、弾座標・所有者・色を要求し、
   `telegraph` はボス座標と SHOT の狙い Y、`hud` は最大 HP・最大体勢・ボス名・防御値、
   `screens` はタイム・被弾数・パリィ数を要求している。
   **`s1` にも `arena` の組み立て表にも無い。**
   `read` の子は「`s1` 以外を読まない」ので**補完不能。**
   → `s1` を完全なスナップショットへ拡張し、`arena` と全 `read` 葉の入力を同じ表で固定する。
   **この変更は `duel`・`read` の両端と全子孫を失効させる**（`README.md` の失効表）

2. **形状展開規則を親へ移したという `duel-split-2` の主張が未達。**
   `duel` の親に残すもの4 は「形状種別の意味論は `duel` が持つ」と**宣言している**が、
   **具体的な展開表は `arena` 1.1 にしか無い。**
   兄弟不可視の `gen.audit` は参照できず、**安全性検査と実戦の当たり判定が別実装になる。**
   → 展開表を `duel` 本文へ移す。**前回の重大5 はまだ閉じていない**

3. **検査 B が「実際に回避可能」の十分条件になっていない。**
   `audit` 1.3 は移動量・`無敵F ≥ 持続F`・フラグ有無を個別に数えるだけで、
   開始位置・回避開始時刻・ロール移動距離・多段・弾道を再現しない。
   `audit` 7-3 自身が「必要条件として使う」と認めている。
   **必要条件を通っただけの攻撃を「有効回答」に数えるので、根の不変条件10 に対して偽陽性がある**

4. **黄の選択規則が内部矛盾している。**
   根の 1.3 は「プレイヤーが安全席に居る時だけ黄を選び、それ以外は非黄を選ぶ」と定める。
   しかし `foe` 1.2 の 3 は「先頭3枚だけを見て、条件を満たす最初の技を選ぶ。**無ければ先頭**」。
   **先頭が不適格な黄なら、それを発動してしまう。**
   `foe` 7-3 は「全部黄のとき」だけ再シャッフル案を出しているが、
   **混成デッキでも先頭3枚が不適格な黄なら同じ事故が起きる。**
   → 「適格な非黄まで走査」「待機」「再シャッフル」のいずれかを、**有限停止条件込みで1つに固定する**

5. **第2形態が検査済みデータを実戦側で変更する。**
   `foe` 7-2 が自認するとおり。`d1` は変異の**記述**しか運ばず、
   **`gen` に「第2形態版を再検査して安全席・回答集合を出力する」契約が無い。**
   → 第1・第2形態の両方を `gen` で確定・検査し、`fight` は**検査済み版を切り替えるだけ**にする。
   **リトライ等の `fight → gen` は除去できているが、第2形態だけ実質的な設計時逆流が残っている**

6. **`g4` / `f2` / `f4` に必須フィールドが無い。**
   - `d1` と `s1` は技ごとの**有効回答集合**を要求するが、`g4` は合否・違反・安全席しか返さない。
     **回答集合の正本が作れない**
   - `foe` 1.3 は「黄判定用のプレイヤー中心を `f4` から受ける」と書くが、
     **`fight` の `f4` の表にプレイヤー位置も安全席も無い**
   - `f2` に SHOT の固定照準や RAIN の発生位置が無い

7. **能力契約が実装値と未決のまま。**`duel` 本文5 の 12 は半幅9・高さ44・歩速3.1・480×270 を
   **決定値**として書き、同じファイルの `boundary_request` が実測（640×360・12×25・半幅6・2.45）を
   記録して**採用値を未決にしている。**実コードも後者。
   **検査 B の基礎なので、±25% 許容では済まない。人間が一方を決める必要がある**

8. **危険度予算とボス主色の意味が親子で変わっている。**
   - 根と `duel` は危険度予算を**ボス全体の総量**とするが、`move` 1.3 は
     `boundary_request` 無しで「**技ごとの帯**」を暫定採用している。**子による祖先決定の再選択**
   - 根の 1.6 は「危険度への寄与が最大の特性」を主色とするが、
     **実装は候補色から `pick()` でランダム選択している。**
     `AS_BUILT.md` §8 の適合評価は**この点を過大評価していた**

9. **`tell` の「子同士の seam が無い」は事実ではない。**
   `telegraph` と `actors` は `palette` の関数を直接呼ぶ。
   **「関数なので seam ではない」という説明は、`precedents.md` の
   「結合を別名にして指標の外へ置いた」（`split-4` の誤り）と衝突する。**
   → `palette` を親所有の共有定数／純関数へ引き上げるか、要求・結果を seam として明記する

**■ 実装しながら直せる — 6件**

- `g1` は「乱数系列を持つのは `roster` だけ」としながら、`move` へ**状態を進める乱数関数**を
  渡している。乱数値の列か `seed + 消費数` を渡す API へ変えれば局所修正で済む
- 固定フォールバックが「短射程・単発・非黄」としか定義されていない。
  **停止証明がこれに依存する**ので、形状・9パラメータ・回答・安全性を**具体値で固定**し、
  `audit` の回帰試験へ入れる
- `palette` の赤の冗長化「危険範囲を太くする」は、
  **`read` が `s1` の座標を変更せず描く契約と衝突する。**
  `actors` の武器シルエット案など、**命中前に見える別チャネル**へ固定する。
  被弾後の画面揺れは予告の冗長化にならない
- HUD のスタミナは、ロール／パリィの必要量の**目盛り**を足せば局所修正で済む
- **操作説明を `screens` と `shell` の双方が所有している。**
  `frame` の「子間 seam 無し」と `shell` 1.1 の画面内操作文が衝突する。
  `s1` の操作割当から**一度だけ生成する担当を親で決める**
- 被験者の受け入れ条件に実行可能な代替が無い。
  `read` 7-1 の「1週間空けた制作者」は**未知の被験者ではない。**
  → **「2026-09-08 までに1名確保、不可なら客観的プローブのみを必須」**と期限付きで縮退条件を決める

**■ 記録だけでよい — 5件**

- ハブ構造自体は成立している（`g1`〜`g4` は全部 `roster`、`f1`〜`f4` は全部 `arena` が端点）。
  ただし**「兄弟同士の線が0本」は不正確。**兄弟間 seam はそれぞれ4本あり、
  正しくは「**非ハブ兄弟間の直接線が0本**」
- `s1` を複数の子が読むのは共有状態だが、読み取り専用なので書き込み競合は無く、
  失効範囲も記載されている。**問題は共有そのものではなく、`s1` が不足していること**
- 深さ2＝機能分割、深さ3＝実装可能な葉として概ね整合。
  **`palette` と `shell` は小さいが、規約に最小サイズは無く、責務・入力・受け入れ条件は存在する。
  小さいことだけを理由に差し戻す根拠は無い**
- **外部監査者が兄弟を横断して読むという根の論証は妥当。**
  兄弟不可視は子ノードの cold start に掛かる制約で、監査者・人間・オーケストレーターには掛からない
- `AS_BUILT` の寸法・6形状・1体ずつ生成・第2形態変異・既存検査の内容は**コードと概ね一致。**
  現行テストも 30,000体を通過する。ただし**安全席・黄選択・ラン全体生成・新しい `s1` は
  `AS_BUILT` 自身が未実装差分としており、葉の主要な新設計まで「実測からの書き起こし」とは
  扱えない**

**確認項目別の結果:**

| 項目 | 結果 |
| --- | --- |
| 1. 親決定・必須入力 | **不適合／判定不能。**人間承認が無く、能力契約・形状規則・`s1` が未確定 |
| 2. 深さの適切性 | **適合。**深さ2＝機能、深さ3＝実装葉 |
| 3. seam・共有状態 | **不適合。**`s1` 欠落、`g4`・`f2`・`f4` 欠落、乱数の隠れた書き込み |
| 4. 受け入れ条件 | **不適合。**検査 B が必要条件のみ、被験者条件に実行可能な代替なし |
| 5. 兄弟不可視 | **不適合。**`telegraph`/`actors` → `palette` と `screens` ⇄ `shell` が未宣言 |
| 6. 自己完結性 | **不適合。**`audit` は親に無い形状表、`read` の葉は `s1` に無い値を必要とする |
| 7. 既存文書との整合 | **部分適合。**`AS_BUILT` の実測部は概ね正確だが、主色評価等に過大主張あり |
| 8. 深さ・子数上限 | **適合。**最大深さ3、最大子数4で上限内 |

**判定: 判定不能**

> 技術的な推奨は「修正のうえ再提案」だが、形式上は判定不能。
> `precheck.md` §1 は親内容の**人間承認**を必須入力とし、
> `README.md` は提案 AI と確認 AI が承認できないと定める。
> **`waiver-1` の委任文自身も「前検査は省かない」と書いている。**
> したがって **Claude による自己承認を親決定の正式承認として扱うことは、
> 現行ハーネスの形式とは両立しない。**

**締切を踏まえた勧告（Codex 記）:**

- **ツリー全体は作り直さない。**次の**7契約だけ**を 2026-09-05 中に凍結する —
  `s1` / 形状展開 / 能力値 / 検査 B / 黄の選択 / 第2形態 / `g4`・`f2`・`f4`
- 人間は修正版の `duel`・`read` と深さ2の分割を**一括で明示承認**し、
  変更を受ける既存の葉を再承認する。**本文の再作文は行わない**
- 最初の実装は「**生成 → 検査 → 1戦 → `s1` → 最小描画**」の**縦切り**にし、
  安全性と ZIP 容量を **2026-09-06 までに実測**する
- **捨てる順序**: 音 → 粒子 → 画面揺れ → 外装の装飾
- **守る順序**: 2回答保証 → 黄の安全席 → 第2形態の再検査 → 予告と当たり判定の同一形状 →
  HP / 体勢 / スタミナの可読性
- 未知の被験者を 2026-09-08 までに確保できなければ、統計的な「有意差」は出荷条件から外し、
  **決定論的なコントラスト・グレースケール・合成18F テストを必須条件にする**

**過去判断との関係（Codex 記）:**

| 前回の指摘 | 状態 |
| --- | --- |
| **重大3**（`fight → gen` の逆向き） | **リトライ・敗北後再挑戦・撃破後の次ボスについては閉じた。**`roster` が全ランを先に作り、`arena` が `d1` を保持して再利用する。**ただし第2形態変異だけ未解決** |
| **重大4**（プレイヤー能力契約） | **構造は実施済み**（親へ引き上げ）だが、**本文値と実装値が衝突したままなので内容面では閉じていない** |
| **重大5**（形状展開と `s1`） | **`s1` の提供者を `arena` とした点は改善。**ただし**展開表が `arena` に残り、`s1` のフィールドも不足**しているので、部分的にしか閉じていない |

> 過去前例は「前検査は後追いで補えるが、下位が進んだ場合は失効を免れない」としており、
> **今回はまさにその状態である。**ただし修正対象を上の7契約に限定し、
> 人間がまとめて再承認すれば、**9日間での実装可能性はまだ残っている。**（Codex 記）

#### 提案側（Claude）による照合 2026-09-04

**重い9件のうち、とくに構造に効く4件を実物と突き合わせた。4件とも成立する。**

| 指摘 | 照合した実物 | 判定 |
| --- | --- | --- |
| **1. `s1` の欠落** | 根の `split-7` の seam 節（技表示 / 戦闘状態 / ライフサイクル / 結果表示 / 操作割当）に**座標が1つも無い**。`actors` 1.1 はプレイヤー・ボス・弾の座標を要求 | **成立。**`read` の葉は動かせない |
| **2. 形状展開表が親に無い** | `duel/index.md:718`「形状種別の意味論は `duel` が持つ」と**宣言のみ**。展開表は `arena` 1.1 にしか無い | **成立。**前回の**重大2（項目名だけで値が無い）と同じ型の再発** |
| **4. 黄の選択の穴** | `foe/index.md:71`「先頭3枚だけを見て、条件を満たす最初の技を選ぶ。**無ければ先頭**」／根の 1.3 の選択時条件 | **成立。**受け入れ条件「利用可能性・違反0件」が**原理的に達成不能** |
| **8 の後半. 主色** | `prismatic-duel/game.js:290` `const hue=pick(r,traits.length?traits:[0])` — **候補集合からランダム** | **成立。**`AS_BUILT.md` §8 の「一致」表が誤り。**差分11 として追記し訂正した** |

**まとめ**:

- **9件のうち3件（1・2・9）は同じ型である** — **「親が持つ」と宣言して、中身を書いていない。**
  Codex は `duel-split-1` の**重大2 で同じことを指摘していた**（「`親に残すもの3` が
  項目名の列挙に留まる」）。能力契約は値まで書いて直したのに、
  **形状展開規則と `s1` では同じ誤りを繰り返した**
- **9 はさらに悪い。**`palette` の呼び出しを「関数であって seam ではない」と書いたのは、
  `precedents.md` に**自分で記録した誤りの型**（`split-4`「結合を別名にして指標の外へ置いた。
  自己申告しても判定は改善しない」）そのものである。**3回目の同型再発**
- **4 は実装由来の見落とし。**`nextMove()` の「無ければ先頭」を `AS_BUILT` からそのまま
  持ってきて、**選択時条件を足したのに fallback を直さなかった**
- **5（第2形態）と 7（能力契約）は、葉が自分で 7 に起票していたもの**であり、
  Codex はそれを「実装を止める」側へ格上げした。**格上げは妥当である** —
  どちらも「間違ったものが出来る」に該当する

### precheck 2026-09-04（深さ3全体・2回目）by Codex — 判定 HOLD

**全文**: [`design/CODEX_REVIEW_2026-09-04.md`](../../CODEX_REVIEW_2026-09-04.md)（人間が実施）

**判定: HOLD — 現在の正本のまま実装フローへ渡してはいけない。**
設計の骨格（`gen → fight → s1 → read`、`roster` / `arena` / `palette` をハブにする切り方）は
維持できる。**全面的な作り直しは不要。**ただし 2026-09-04 の「凍結」が
**有効版・子への振り分け・葉の本文へ一貫して反映されておらず**、
実装者が閉包だけを読むと**異なる実装へ到達する。**

#### 反映結果（2026-09-05・全10件）

| # | 指摘 | 対応 | どこ |
| --- | --- | --- | --- |
| **P0-1** | 有効な版と承認状態が一意でない（frontmatter・有効表・本文・子の参照がばらばら） | **全20ノードの版を切り直してそろえた。**旧版は `superseded` | 全ノード |
| **P0-2** | 凍結した新ルールと旧仮置きの**二重記述** | `audit` 7-3 / `foe` 7-2・7-3 / `arena` 5-2・5-6 / `telegraph` 1.4・3 / `shell` 1.1 / `screens` 7-3・7-4 を**履歴へ降格** | 6ノード |
| **P0-3** | フォールバック技の `1.2.1` が**実在しない**（停止証明が成立しない） | `roster` に **1.2.1 を新設**し9パラメータ・回答集合・安全席の要否・第2形態の扱いを記載。**回帰試験を受け入れ条件へ追加** | `roster` |
| **P0-4** | 黄技選択の**有限停止が成立しない**（未使用の残りが不適格な黄1枚でデッドロック） | 「**山札を復元して全技から選ぶ**」段を追加。**停止性テストを受け入れ条件へ追加** | `foe` |
| **P0-5** | **SHOT の時空間仕様が無く**、`audit B` が判定できない | `duel` の展開規則に **4.1 SHOT の完全仕様**（発射位置・寸法・寿命・追尾更新式・段ごとの発射時刻） | `duel` |
| **P0-6** | seam の送受信が**端から端まで一致していない** | `move` の入出力を `g1`/`g2` へ一致。`arena` の `s1` 組み立て表を根の `split-8` と同じ集合へ。`f1` に最大 HP / 最大スタミナ、能力契約にも追加。`f2` の欠落は `d1` から取ると明記 | 4ノード |
| **P0-7** | 根の生成境界（硬直・パリィ可能性を「生成しない」）が**子の実装契約と正面から矛盾** | 根の 1.2 を**2軸**（生成の対象か / 戦闘中に変わるか）へ。硬直と宣言は**生成する**側 | 根 |
| **P0-8** | 被験者未確保時の縮退を**子が勝手に決めていた** | **根が 4.1 として決めた**（2026-09-08 判定）。`read` は継承するだけ | 根・`read` |
| **P1-9** | `implement` ロールが無く、9月8日の期限例外も未発火 | **`implement` を作らず、人間が直接実装へ移ることを根で明示承認。**`deferred` を全件解除 | 根 |
| **P1-10** | 現行契約だけを短く読める入口が無い | **全20ノードの先頭に「📍 現行契約への索引」を追加** | 全ノード |

#### 再判定の合格条件に対する自己申告

| # | 条件 | 状態 |
| --- | --- | --- |
| 1 | 全20ノードで有効な決定・分割・親参照が一意で、失効版が `active` でない | ✅ 機械的に検証した |
| 2 | P0 の二重記述が無く、フォールバックと黄技選択に有限停止の根拠がある | ✅ 両方とも具体値と手順を本文へ置き、受け入れ条件に回帰試験を足した |
| 3 | SHOT を含む全6形状を、同じ規則で `audit` と `arena` が扱える | ✅ `duel` の展開規則に一本化。**実行者と所有者を分けて書いた** |
| 4 | `g1`〜`g4`、`f1`〜`f4`、`d1`、`s1`、`t1`/`t2` の送受信フィールドが一致する | ✅ 送信者・親の表・受信者・`s1` 組み立て表をそろえた |
| 5 | 修正後の版に**別実装 AI の前検査**と人間の承認が記録され、実装者の入口が定義されている | ⚠️ **人間の承認と実装者の入口は済み。前検査は未実施**（この版そのものは検査を受けていない） |

**5 だけが未達である。**この反映版に対する Codex の再検査は回していない。

#### 再監査の状況（2026-09-05）

**Codex が使用上限に達した**（リセット 2026-09-07 12:15）。**再監査は回せていない。**

代わりに提案側（Claude）が、Codex の再判定条件5点のうち**機械的に確かめられる1〜4を自己検証した。**

| # | 条件 | 自己検証の結果 | 方法 |
| --- | --- | --- | --- |
| 1 | 全20ノードで有効な決定・分割・親参照が一意で、失効版が `active` でない | ✅ **不整合0件** | frontmatter / 有効表 / 子の `parent_decision_ref` を突き合わせるスクリプト |
| 2 | P0 の二重記述が無い | ✅ **現行命令として読める旧仮置き0件**（5箇所すべて履歴ブロック内） | 旧仮置きの文字列を検索し、`>` 引用・`~~`・「旧文」の外に出ていないか判定 |
| 2 | フォールバックと黄技選択に有限停止の根拠がある | ✅ 両方とも本文に具体値と手順があり、受け入れ条件に回帰試験がある | 目視 |
| 3 | SHOT を含む全6形状を、同じ規則で `audit` と `arena` が扱える | ✅ `duel` に展開表と SHOT 4.1、両子とも参照 | 検索 |
| 4 | `g1`〜`g4` / `f1`〜`f4` / `d1` / `s1` / `t1`・`t2` のフィールドが一致 | ✅ **`s1` は10/10、他8項目すべて OK** | 送信者・親の表・受信者の3点で同じ語を検索 |
| 5 | 修正後の版に**別実装 AI の前検査**と人間の承認が記録され、実装者の入口が定義されている | ⚠️ **前検査だけ未達** | — |

**自己検証で1件見つかって直した**: `duel` の 7-1 が
「仮置き: 山札を引き直す。`provisional: true`」を**現行の命令の形で残していた**
（`foe` 1.2 で凍結済みなのに）。**P0-2 と同じ型の見落としが1件残っていた。**

> ⚠️ **自己検証は前検査の代わりにならない。**
> `harness/README.md`「**提案した側は承認できない**」「確認する AI は提案した AI と別実装であること
> （同じモデルだと同じ見落としをする）」。
> 上の1〜4は**機械的に確かめられるものだけ**であり、
> **設計判断の妥当性（保守的検査が厳しすぎないか、停止性の論証が正しいか等）は検証していない。**
> **条件5は未達のままである。**

### precheck 2026-09-05（再監査）by Codex — 判定 HOLD

**再判定の合格条件は 0/5。**重大7件。**すべて反映した**（下表）。

| # | 条件 | Codex の判定 | 反映後 |
| --- | --- | --- | --- |
| 1 | 版・親参照が一意で失効版が active でない | **不合格**（`split-8` が `decision-8` を前提にしたまま `decision-9` と同時に active。承認も版を特定していない） | `decision-10` / `split-9` を切り直し、**`approval-2` で対象版を表に明示** |
| 2 | フォールバックと黄技選択に有限停止の根拠 | **不合格**（フォールバックは `audit C` に必ず落ちる／黄技は段5でループ） | 両方**構造を変えた**（下記 重大2・3） |
| 3 | 全6形状を同じ規則で `audit` と `arena` が扱える | **不合格**（SHOT の `PARRY` が3者で不一致） | `move` の SHOT に `PARRY` を立て、**判定経路を `m[F] & PARRY` 1本へ** |
| 4 | seam のフィールドが一致 | **不合格**（`g3`/`g4`/`t1`/`d1` の意味論がずれ） | 4本とも親の表へ一致。**`d1` の安全席に座標基準を追加** |
| 5 | 別実装 AI の前検査と版を特定した人間承認 | **不合格** | **前検査は実施済み（これ）。承認は `approval-2` で版を特定** |

#### 重大7件の反映

| # | 指摘 | 何が問題だったか | 直し方 |
| --- | --- | --- | --- |
| **1** | `decision-9` と `split-8` は有効な組ではない | `split-8` 自身が `decision-8` を前提にしており、割当本文も `decision-8` のまま。**承認は9月4日の包括発言を9月5日の決定へ遡及適用**していた | `decision-10` / `split-9` を切り、**`approval-2` で対象版を表にした** |
| **2** | **フォールバックは `audit C` に必ず落ちる** | 危険度 **−6** で帯域下限未満なのに「帯域検査の対象外」と自己宣言。**`audit` C は「危険度が limits の外」を無条件の不合格条件**にしており、`roster` は合否を上書きできない（本文5-5）。**解釈の余地がない直接矛盾** | **例外を作らず、帯域に入る値へ変えた。**威力3 / 予備動作54F / 硬直32F → **危険度 5**（3 tier すべての帯域内） |
| **3** | **黄技選択は有限停止しない** | 段5 の「12F 待って再走査」に状態更新が無く、同じ状態へ戻り続ける。**「全技が黄になることはない」という論証も誤り** — 黄は `W ≤ 2`（24F と 36F）だが、`audit` C が制限するのは **24F（`W=1`）だけ**。**全技を `W=2` にできる** | 段5 を**フォールバック技を出す**へ変更。**ループが消え、最大2回の走査で必ず攻撃へ到達する。**`gen` の生成条件に停止性を依存させるのをやめた。あわせて**同技3連続を禁止**（公平性の弱め方を明示） |
| **4** | SHOT は仕様追加だけでは閉じていない | 共通仕様は「パリィ可能」、`move` の宣言は「移動」だけ、`audit`/`arena` は `m[F] & PARRY` を見る。**同じ SHOT を両者が別の回答集合として扱う** | `move` の SHOT に **`PARRY` を立てた。**実装は `m[F]` と別に `parry:1` を持つが、**ツリーは経路を1本にする** |
| **5** | **`d1` の安全席に座標基準が無い** | 絶対 X 区間なのに基準のボス位置・向きが無い。**CHARGE の包絡は現在のボス X と向きで変わる。**`foe` は再計算禁止なので**適用方法が定義不能だった** | **相対オフセット＋向きの組で持つ。**`foe` は `ボス中心X + 向き × オフセット` を計算するだけ（**平行移動と反転であって再計算ではない**）。戦闘領域の端で切る |
| **6** | 根の2軸化がまだ内部矛盾 | 「**展開済み当たり判定は戦闘中に動かない**」が CHARGE（ボスが前進）と SHOT（弾が飛ぶ）と衝突 | 軸 B を**2行に分けた。**固定するのは**パラメータ・確定した照準値・運動規則**で、**ワールド座標は規則どおり動く** |
| **7** | **HP=8 が難易度予算と接続されていない** | 実装は3人 HP 合計25＋自動交代。1人 HP 8 は**約68%減**。`fight` は「ボスの予算で吸収」とするが、**`gen` は合計上限を未決定のまま委任**しており対応式が無い。**`audit B` は HP を使わないので公平性検査で検出できない** | **HP と予算を同時に凍結。**HP **14**（実装比56%）、**ボス合計上限 `24 + tier × 6`**（帯域上限の8割）。判定基準は台本入力の平均被弾が HP の50%未満 |

#### 「実装しながら直せる」5件

| 件 | 対応 |
| --- | --- |
| `move` の「乱数関数」残存記述 | ✅ **値列へ統一。**受け入れ条件も「同じ値列を2回渡して一致」へ |
| `foe` の `f2` 矩形記述 | ✅ 「危険範囲の矩形も作らない。**ボス本体の矩形**を渡す」へ |
| 根 7-2 の「代替判定が無い」旧文 | ⏸ **未対応**（根の 4.1 で縮退を決めたので、7-2 の旧文は履歴へ落とすべき） |
| `AS_BUILT` の生成境界・2層予算 | ⏸ **未対応** |
| 山札復元時の連続同技の上限 | ✅ **3連続を禁止**と明記 |

**残る2件は記録のみで、実装を止めない。**

> 🧭 **Codex の指摘で最も重かったのは重大3である。**
> 前回「山札に非黄が1つでもあれば走査は必ず当たる」という論証が対象を取り違えていると
> 指摘され、直したつもりだった。**今回は「全技が黄になることはない」という論証が
> `audit` C の実際の条件（24F のみ制限）を読み違えていた。**
> **同じノードの同じ論証で2回続けて誤った。**
> **`gen` の生成条件に `foe` の停止性を依存させる形そのものが誤りだった** —
> フォールバックで単独に停止させる形にして、依存を断った。（2026-09-05）

## boundary_requests

<!-- 子孫からの境界変更の起票。追記のみ。
     open は非権威的。他の子はこれを親の決定として読んではいけない -->

- from: `duel.fight.avatar` / date: 2026-09-04
  対象: `duel` の本文5 の 12（プレイヤー能力契約）の値
  理由: 契約は AI が直感で置いた `provisional` 値で、`prismatic-duel/` の実測と
    突き合わせていない（`duel-split-2`「この提案自体への疑い」3 が自認）。
    実測は **内部解像度 640×360 / 当たり判定 12×25（半幅 6）/ 歩行 2.45 px/F /
    ジャンプ到達 59px**。契約は **480×270 / 半幅 9・高さ 44 / 3.1 px/F / 52px**。
    差が ±25% の調整幅を超えるため、`avatar` では直せない。
    **ロール無敵 11F とパリィ受付 6F は一致している。**
  影響: **`gen.audit` の検査 B（回答が2つ）は契約値を使う。**
    直るまで検査結果は暫定である
  status: **accepted**（2026-09-04）

  **受理した。**本文5 の 12 を**実測値へ差し替えて凍結した**（`decision-3`）。
  Codex の深さ3監査も同じ点を**着手前7**として挙げ、
  「検査 B の基礎なので ±25% 許容だけでは済まない。人間が一方を決める必要がある」とした。
  **人間の承認（`root@2026-09-05-approval-2`）に基づき、実装値を採る。**
  ゲーム側を契約値へ変える案は採らない — **実装が既に動いており、直す理由が無い。**

  > 🧭 **AI**: これは `duel-split-2` が「未検査リスク」として記録していたもの
  > （能力契約を動かすと `gen` の検査が古くなる）が、**想定と逆向きに現れた** —
  > 動かしたのではなく、**最初から合っていなかった。**（2026-09-04）
  >
  > **処理には `duel` の cold start が要る**（`harness/README.md`「親を再開する起票」）。
  > 受理して本文5 の 12 を直すと `decision-2` は失効し、`decision-3` の承認が要る。
  > **`gen` と `fight` の部分木は、能力契約が「継承する制約」に入っているので
  > 失効範囲に含まれる**（`harness/README.md` の失効表）。
  >
  > **残り9日でこれを回すか、実測値で読み替えて実装を進めるかは人間の判断である。**

## seam_inbox

<!-- 子同士の seam 成果物はここへ追記され、親が権威化して宛先の子へ降ろす -->

## 承認証跡

### 現在有効なもの

| 種別 | 版参照 | status |
| --- | --- | --- |
| **決定** | `duel@2026-09-05-decision-8` | **active** |
| **分割** | `duel@2026-09-05-split-5` | **active**（承認済み・`children-created`） |

**次にやること**: `gen` / `fight` は作成済み。Codex が復帰したら
**`duel-split-2` の前検査を後追いで回す**（`split-1` のときと同じ手順。
指摘が出たら `review.md` §3 により承認が失効しうる）。

**`duel-split-1` の前検査（重大8件・軽微5件）への対応状況:**

| # | 指摘 | 対応 |
| --- | --- | --- |
| 重大1 | 承認者が人間でない | **未対応。**解除できるのは人間だけ。`design/README.md` の「未処理」へ記録した |
| 重大2 | 共有定数が項目名だけ／本文6と衝突 | 本文5 の 12 に**値まで記載**（`provisional`・±25%）。本文6 から該当分を引き上げ |
| 重大3 | `fight → gen` の輪 | **`gen` がラン全体を一度に生成する**（`duel-split-2` 変更1）。`d1` を `run-definition` へ |
| 重大4 | `gen` が宣言の有効性を判定できない | 能力契約を親へ（本文5 の 12）。`gen` は `fight` を待たずに検査できる |
| 重大5 | 形状IDだけ／`fight` の「提供 seam なし」 | `d1` に展開パラメータ一式。展開規則は親。`fight` を `s1` の提供者と明記 |
| 重大6 | 公平性が親の範囲を変更／2回答を検査していない | 本文4 を **公平性 A（黄のみ）と B（全技・回答2つ）に分割** |
| 重大7 | 学習が代理条件に差し替わっている | 根の判定方法を主に戻し、台本比較は**補助**へ降格 |
| 重大8 | 承認が根の `deferred` の再開条件そのもの | **先に root を再開**（`root@2026-09-04-decision-7` / `split-7`）。実装基盤を決定 |
| 軽微1 | `fight` の責任文とラン構造が不一致 | 責任文を「確定したランを最後まで走らせる」へ |
| 軽微2 | 全称で書いて標本で判定 | 停止性を**構造上の根拠＋標本**へ。公平性は標本と明記 |
| 軽微3 | 50% 閾値を子が変えられるように書いた | `boundary_request` で `duel` を再開すると明記。閾値は補助条件の側だけ |
| 軽微4 | 根の消し残し2件 | 根で修正済み（`split-7` / `decision-5` の status） |
| 軽微5 | 容量を全量配り切っている | 親の取り分 800 bytes を確保（6,800 / 10,900 / 800） |

**12 / 13 件に対応した。**残る重大1（承認者）は規約の問題で、AI では解除できない。

---

---

- **`duel@2026-09-05-decision-8`** / 承認者: **人間（DaichiFukuhara、`root@2026-09-05-approval-2` により）**/
  日時: 2026-09-05 / 種別: `decision` / 承認者の種別: **人間** / **status: active**
  対象: 本文1〜7（課題の範囲と、その課題に対する解決法）

  `decision-1` からの差分は**すべて Codex `duel-split-1` 前検査への対応**であり、
  提案側の創作は無い。

  | 差分 | 出所 |
  | --- | --- |
  | 解決法の第3層を **3a（安全席・黄のみ）と 3b（回答2つ・全技）へ分割** | 重大6 |
  | 受け入れ条件「公平性」を **A と B の2条件へ**。A の対象を根と同じ**黄の技**に戻した | 重大6 |
  | 受け入れ条件「学習」を**根の判定方法へ戻し**、台本比較を補助へ降格 | 重大7 |
  | 本文5 に **12（プレイヤー能力契約）**を新設。値まで記載・`provisional`・±25% | 重大2・4 |
  | 本文6 から、公平性の検査に効くプレイヤー性能5種を引き上げ | 重大2・4 |
  | 本文5 の 8（実装基盤）を `deferred` から `prismatic-duel/` へ | 重大8・根の 6.1 |
  | 停止性の判定を**構造上の根拠＋標本**へ。50% 閾値の変更経路を明記 | 軽微2・3 |

  **重大6 がいちばん重い。**`decision-1` は「生成されたすべての技が安全席の検査を通る」と
  書いており、根の 1.3（**黄の技だけ**）の範囲を変更していた。
  そのままだと**移動して避ける技・ジャンプで越える技・ロールで抜ける技が
  1つも生成できず**、根の 1.4 が挙げる回答手段4つのうち3つが死ぬ。
  **手続き上の逸脱ではなく、ゲームが別物になる誤りだった。**

  **`decision-1` は失効**（承認後に本文を触ったため。`review.md` §3）。
  内容は `decision-2` に入っている

- **`duel@2026-09-05-split-5`** / 承認者: **人間（DaichiFukuhara、`root@2026-09-05-approval-2` により）**/
  日時: 2026-09-05 / 種別: `split` / 承認者の種別: **人間** / **status: active**
  対象: 分割提案 `duel-split-2`
  （子2つ `gen` / `fight`、seam は `d1.run-definition` 1端点・片方向）

  ### ⚠️ 前検査を経ずに承認した。理由を記録する

  `review.md`「重大指摘を残したまま承認する場合は、その理由を承認証跡へ書く」に従う。

  **事実**: `duel-split-2` は前検査を**1度も受けていない。**
  `duel-split-1`（前身）は受けており、**判定は「判定不能」・重大8件・軽微5件**。
  そのうち12件を反映した版が `duel-split-2` である。
  `split.md`「提案を検査後に編集したら、前検査は失効し、やり直す」に照らせば、
  **この提案の前検査は失効している。**

  **理由**:

  1. **Codex のリソースが尽きた**（2026-09-04）。前日の使用上限に続き、
     `duel-split-1` の検査1回を回した時点で再検査の手段が無い
  2. **残り9日で実装が未着手。**根の本文7-5 の打ち切り条件は 2026-09-02 に
     既に満たされており、打ち切りの目安 2026-09-08 が近い。
     前検査を待って止めるコストが、指摘を見逃すリスクを上回ると判断した
  3. **反映した12件は、いずれも Codex 自身が示した処方である。**
     とくに重大3（輪）は「全ランを開始前に生成する案なら、`d1` の『1体』と子責任を
     その形へ変更する必要がある」という具体的な指示をそのまま採った。
     **提案側の創作ではないぶん、外し方が小さい**（根の `split-6` を承認したときと同じ論法）
  4. **深さ3の葉に入る内容は、大半が `design/AS_BUILT.md` の実測値**である。
     `prismatic-duel/` として**既に動いていて 30,000体のテストを通っているもの**を
     書き起こしたので、設計の創作部分が少ない

  **残るリスク（未検査）**:

  - **重大1（承認者が人間でない）は未対応のまま。**`waiver-1` は人間の明示の委任だが、
    Codex は「提案者自身への承認委任を認める前例ではない」と指摘している。
    **解除できるのは人間だけ**なので、`design/README.md` の「未処理」に記録した
  - `duel-split-2` で新たに書いた4変更（ラン全体生成 / 能力契約 / 形状の展開規則 /
    `s1` の提供者明記）は、**別実装の目を一度も通っていない**
  - とくに**能力契約の「±25% まで子が調整してよい」**は、
    調整後に `gen` の公平性検査が無効にならないかを検証していない。
    **重大3・4 の再発になりうる**（`fight` が値を動かすと `gen` の検査結果が古くなる）

  > 🧭 **AI**: `duel-split-1` を前検査なしで承認しなかった判断と、ここで承認する判断は
  > 矛盾しない。**前者は前検査を回せる見込みがあった**（Codex のリセットが同日中）。
  > **後者は見込みが無い。**待つ対象が無いなら、待つことは締切を削るだけである。（2026-09-04）

---

- **`duel@2026-09-03-decision-1`** / 承認者: **人間（DaichiFukuhara、`root@2026-09-05-approval-2` により）**/
  日時: 2026-09-03 / 種別: `decision` / 承認者の種別: **人間** /
  **status: superseded（2026-09-04。`decision-2` へ）**
  対象: 本文1〜7（課題の範囲と、その課題に対する解決法）

  **決定の要点**: 公平性の保証を**危険度予算から安全席の検査へ移した。**
  根の `1.3` は禁止組み合わせの列挙で不変条件10を守ろうとしていたが、
  Codex の前検査3回で**列挙では塞ぎきれない**ことが繰り返し示された。
  予算と禁止列挙は**引き直しを減らす最適化に降格**し、保証は検査器が持つ。

  **前検査を経ていない決定である。**`precheck.md` は分割提案への前検査を求めており、
  決定そのものへの前検査は規定されていないので規約違反ではない。ただし
  **本文は AI 起草・AI 承認**であり、別実装の目が入っていないことを記録しておく。
  Codex が復帰したら `duel-split-1` の前検査とあわせて本文も見せる

- **`duel@2026-09-03-split-1`** / **status: 修正のうえ再提案（2026-09-04）**
  種別: `split` / 対象: 分割提案 `duel-split-1`（子2つ `gen` / `fight`、seam 1端点・片方向）

  **2026-09-03 時点で Codex が使用上限に達しており（リセット 23:36）、前検査を回せなかった。**
  `split.md`「前検査を経ていない提案を人間へ出さない」に従い、**承認せずに pending で置いた。**
  根の `split-6` は締切を理由に前検査なしで承認したが、
  **同じことを各深さで繰り返すと、別実装の目が一度も入らないまま深さ3へ着く。**
  ここでは止めた。

  **2026-09-04、Codex が復帰したので前検査を実施した。判定は「判定不能」**
  （重大8件・軽微5件）。提案側の照合でも13件とも成立したので、**承認しない。**

  > 🧭 **AI**: **止めた判断が正しかった。**`split-6` と同じく締切を理由に承認していたら、
  > `fight → gen` の輪（重大3）と、根の 1.3 の範囲を変更した公平性条件（重大6）を
  > 抱えたまま深さ3へ着いていた。とくに重大6 は手続きではなく**ゲームが痩せる実害**で、
  > 「移動して避けるしかない技」が1つも生成できなくなる。（2026-09-04）
