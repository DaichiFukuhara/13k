# design/ — AIDE 設計ツリー

このゲームを AIDE の設計ハーネスで作り直すためのツリー。

| パス | 中身 |
| --- | --- |
| `harness/` | 規約と役割。AIDE リポジトリからの複製（`harness/PROVENANCE.md` に出所） |
| `tree/` | このゲームの設計ツリー。ディレクトリがそのまま木、ノードは `index.md` 1枚 |

## 入り方

新しいチャットを cold start して、こう渡す。

```
design/harness/README.md に従って design/tree/<ノードのパス> を進めてください。
閉包（自ノードと祖先チェーンの index.md）を先に読んでください。
```

これで始められないなら、原因はチャットではなく**ツリーに書かれていない情報**にある。

## 現在地

| ノード | 深さ | status | 次にやること |
| --- | ---: | --- | --- |
| `tree/index.md` | 0（根） | `questions-done` | **人間の決定承認**。承認まで子ノードを作らない |

- 本文1〜7は **AI 起草の未承認案**（2026-08-31・起草 Claude）。人間が原文を書いた箇所は無い
- 承認されるまで `roles/split.md` の分割提案へ進めない（前検査も判定不能になる）
- 未解決の問い2件が根の 7 と blockquote `❓` に立っている
