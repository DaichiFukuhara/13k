# Random Duel Rainbow — Design Documentation

## Start here

The [main README](../README.md) and [development guide](../docs/DEVELOPMENT.md)
provide an English introduction. The specifications and decision history below
are primarily in Japanese and retain their original wording.

- [Current specifications](spec/README.md): generation, combat, presentation, interface and packaging.
- [Design tree](tree/index.md): decisions and their precedence. Read the latest amendments first.
- [September 13 verification](PLAYTEST_2026-09-13.md): parry response and attack timing checks.
- [AIDE harness](harness/README.md): the design process, roles and provenance.

The remaining index and progress snapshot were written during early development.
Their dates, old paths, sizes and unresolved items describe that historical state;
they are not the current release status.

## Historical index (Japanese)

このゲームを AIDE の設計ハーネスで作り直すためのツリー。

| パス | 中身 |
| --- | --- |
| [`spec/`](spec/README.md) | **仕様だけを書いた設計書。**判断の経緯・却下案・承認の記録を含まない。**実装を読むならここから** |
| [`SUMMARY.md`](SUMMARY.md) | **人間向けの要約。**ゲームの内容・現在地・未決事項を1枚で。**正ではない**（正はツリー） |
| [`AS_BUILT.md`](AS_BUILT.md) | **既に動いている `prismatic-duel/` の逆翻訳。**決定ではなく観測記録。**§8 にツリーとの差分表**がある |
| [`MOVE_IDEAS.md`](MOVE_IDEAS.md) | **技のアイデア集。**2Dボスアクションのパターンを9要素へ翻訳したもの。決定ではなく素材 |
| `harness/` | 規約と役割。AIDE リポジトリからの複製（`harness/PROVENANCE.md` に出所） |
| `tree/` | このゲームの設計ツリー。ディレクトリがそのまま木、ノードは `index.md` 1枚 |

| [`CODEX_REVIEW_2026-09-04.md`](CODEX_REVIEW_2026-09-04.md) | **2回目の監査の全文**（判定 HOLD）。反映結果は `tree/duel/index.md` の precheck 節 |

**全体を把握したいなら [`SUMMARY.md`](SUMMARY.md) から読む。**`tree/index.md` は
2,800行を超えており、その大半が分割提案と前検査の履歴である。
**各ノードの先頭に「📍 現行契約への索引」があり、現行だけを一意にたどれる。**

## 現在地

**2026-09-05: 設計は深さ3まで完了。監査3回。実装に着手した（交代削除・凍結値の反映）。**

| | |
| --- | --- |
| 有効版 | `root@2026-09-05-decision-10` / `split-9` / 承認 `approval-2` |
| 監査 | 3回とも **HOLD**。3回目の重い指摘は反映済み |
| 実装 | **ZIP 8,549 bytes（64.2%・余裕 4,763）。**`test.mjs` 30,000体 pass |
| 実装済み | 1人化・HP14・合計予算 `24+tier*6`・撃破 14〜29秒/体・スタミナ目盛り・操作説明の単一定義化 |
| **未実装** | **安全席検査 / 回答2つの検査 / 黄の選択時条件 / ラン一括生成 / `s1` 分離 / `arena` ハブ / `palette` 分離 / 主色の寄与最大 / SHOT の `PARRY`** |

<!-- この節が「進捗の観測結果の置き場」である（root の split-7「進捗は報告しない。
     ツリーを直接読む」）。子から報告を受ける経路は作らないと決めたので、
     ここは人間またはオーケストレーターがツリーを読んで書く -->

```
tree/
  index.md                      0 根        children-created  ✅ decision-9 / split-8
    duel/                       1 課題      children-created  ✅ decision-4 / split-3
      gen/                      2 解決法    children-created  ✅ シードから公平なランを作る
        move/                   3 システム  ✅ 技1つを作る（純関数）
        audit/                  3 システム  ✅ 公平性を検査する（純関数・副作用なし）
        roster/                 3 システム  ✅ 手順を回して組み立てる（唯一の状態）
      fight/                    2 解決法    children-created  ✅ 確定したランを走らせる
        avatar/                 3 システム  ✅ プレイヤー1人
        foe/                    3 システム  ✅ ボス1体（選択時条件を持つ）
        arena/                  3 システム  ✅ 両者を突き合わせる（展開の唯一の場所）
    read/                       1 課題      children-created  ✅ decision-3 / split-2
      tell/                     2 解決法    children-created  ✅ 戦闘画面の中身
        telegraph/              3 システム  ✅ 予告と危険範囲（座標を計算しない）
        palette/                3 システム  ✅ 色の語彙と冗長化の規則
        actors/                 3 システム  ✅ 人と物（姿から性格を推測させる）
      frame/                    2 解決法    children-created  ✅ 画面の枠
        hud/                    3 システム  ✅ 戦闘中の情報
        screens/                3 システム  ✅ 画面の行き来と操作の提示
        sound/                  3 システム  ✅ 音（情報の主経路にしない）
        shell/                  3 システム  ✅ 器とビルド（容量の唯一の観測点）
```

`ship/` は 2026-09-01 の案 B（`ship` を親へ畳む）により削除した。
提出可能・一意・期限は根が持つ。

### seam の全体

**設計段階の兄弟間 seam は、どの階層でも「ハブが1つ」か「片方向1本」に収まっている。**

| 階層 | seam | 形 |
| --- | --- | --- |
| 根 | `s1.presentation-state`（`duel` → `read`） | **片方向1本** |
| `duel` | `d1.run-definition`（`gen` → `fight`） | **片方向1本** |
| `gen` | `g1`〜`g4` | **`roster` がハブ。**`move` と `audit` は互いを知らない |
| `fight` | `f1`〜`f4` | **`arena` がハブ。**`avatar` と `foe` は互いを知らない |
| `read` | `r1.screen-state`（`frame` → `tell`） | **片方向1本** |
| `tell` | `t1` / `t2` | **`palette` がハブ。**`telegraph` と `actors` は互いを知らない |
| `frame` | **無し** | 4子は `s1` と親の定数だけを読む |

**輪が1つも無い。**`split-1` の 5 ID（実際は 8 端点）から、
機構を足すのではなく**剥がして**ここまで減らした。

### ⚠️ 現行版は前検査を経ていない

**承認は人間（`root@2026-09-04-approval-1`）が済ませている。**
`waiver-1`（AI への委任）は深さ3到達で終了した。

| 版 | 前検査 |
| --- | --- |
| 深さ3の初稿（2026-09-04） | ✅ **1回目**（判定不能・重大8件） |
| 7契約の凍結（2026-09-04） | ✅ **2回目**（判定 **HOLD**・P0 8件 / P1 2件） |
| **2回目を反映した現行版**（2026-09-05） | ❌ **未実施** |

**Codex の再判定条件のうち、これだけが未達である。**

**葉の本文の大半は [`AS_BUILT.md`](AS_BUILT.md) の実測**（既に動いていて 30,000体の
テストを通っているコード）なので、設計上の創作は限られている。
**創作した部分だけを列挙すると次の5つ:**

1. `gen.audit` の A（安全席）と B（回答が2つ）— **実装に1行も無い**
2. `gen.roster` の**ラン全体一括生成** — 実装は1体ずつ
3. `fight.foe` の**黄の選択時条件** — 実装は距離しか見ない
4. `fight` の **`arena` をハブにする構造** — 実装は直接ぶつかる
5. `tell` の **`palette` を独立させる**構造 — 実装は色が描画に埋まっている

### 深さ3を書いて見つかった穴

**書き下ろす過程で、設計の穴が5つ出た。**すべて該当ノードの 7 に記録してある。

| # | 穴 | 記録場所 |
| --- | --- | --- |
| 1 | **プレイヤー能力契約の値が実測とずれている**（内部解像度・当たり判定・歩行速度・ジャンプ到達）。差が ±25% を超えるので `boundary_request` を起票 | `duel/fight/avatar` 7-1 |
| 2 | **第2形態変異が `gen` の検査結果を古くする。**変異後の技は検査されていない | `duel/fight/foe` 7-2 |
| 3 | **「高威力 → 危険範囲の厚み」が実装に無い。**赤だけ色以外の手段が欠けている | `read/tell/palette` 7-4 |
| 4 | **スタミナ不足が「足りない」と分かる表示が無い。**根の 1.5 の判断材料になっていない | `read/frame/hud` 7-2 |
| 5 | **キー割り当ての持ち主が決まっていない。**`screens` は `duel` が持つ前提だが、`avatar` は実装へ委任している | `read/frame/screens` 7-4 |

**1 は `boundary_request` として `duel` へ起票済み。**残り4件は該当ノードの担当。

**子を開く順序: `duel` → `read`。各階層でも 1番目 → 2番目 が振り分けに書いてある。**

## 決まったこと（2026-09-04）

- **実装基盤: `prismatic-duel/`**（コードと build 環境のみ。ゲーム設計はツリーが正本）
- **不採用物は削除せず、無効と明記して残す。**一覧はリポジトリ直下の
  [`README.md`](../README.md)。受け入れ条件「一意」はそこで判定する

どちらも人間の決定。**削除は `waiver-1` の委任外**なので AI では決められなかった。

## 前検査を5回回した。まだ1度も通っていない

| 回 | 対象 | 結果 |
| --- | --- | --- |
| 1 | `split-1`（**承認済みだった**もの） | 重大5・軽微2 / **修正のうえ再提案** |
| 2 | `split-3`（案 B） | 重大8・軽微3 / **判定不能** |
| 3 | `split-4`（案 B′） | 重大8・軽微4 / **判定不能** |
| 4 | `split-6`（機構を剥がした案） | 重大4・軽微4 / **判定不能**（全件反映後、2回目は未実施のまま承認） |
| 5 | **`duel-split-1`**（深さ1 → 深さ2） | **重大8・軽微5 / 判定不能** |

**5回とも実物の欠陥が出ている。**提案側（Claude）の照合でも、毎回すべて成立している。

### 提案側が繰り返している誤りの型

`precedents.md` 2026-09-01 / 09-04 に記録。**同じ型が深さを変えて再発している。**

1. **「親へ移す」を「結合が消える」と取り違える。**`split-3` は受け入れ条件を根へ移して
   結合が消えたとしたが、根の閉包に子は含まれない。
   **`duel-split-1` でも再発**した — プレイヤーの当たり判定サイズを親へ移して
   `fight → gen` の逆向きを消したつもりが、ジャンプの高さ・滞空・ロールの無敵
   （「回答手段の宣言」を検証するのに要る値）は `fight` に残っていた
2. **結合を別名にして指標の外へ置く。**`split-4` は報告経路6端点を
   「兄弟間 seam ではない」として2端点で評価した。Codex 曰く
   「**自己申告しても判定は改善しない**」
3. **人間の判断を、技術的に正しい理由で読み替える。**理由が正しくても読み替えてよいことにはならない
4. **担当がいるように見せかけた割り当て。**子名は付いているが、
   判定する入力・実行者・統合責任が無い（`split-1` 重大4 →
   `duel-split-1` 重大6・7 で再発）

### 前検査が直したのはハーネスだけではない

ゲーム本体も直っている。根の 1.3（公平性の保証）は**3回書き直された**。

| 回 | 指摘 | 直した内容 |
| --- | --- | --- |
| 2 | 「短い予備動作の代償が長い予備動作」という論理矛盾 | 特性軸と代償軸を分離 |
| 3 | 「射程外に立てば安全」が追尾・多段・接近後攻撃で破れる | **危険包絡**を定義して生成時に検査する形へ |
| 3 | 安全席は存在するがプレイヤーが包絡内から始まる | **選択時条件**（黄はプレイヤーが既に安全席に居る時だけ出せる） |

3 は結果としてメカニクスになった。プレイヤーから見ると
**「安全な位置に居れば速い技が来る、居なければ来ない」** — 位置取りが意味を持つ。

## 次にやること

`duel-split-1` の前検査（重大8件）への対応は**12/13 件が完了した。**

1. ~~root を再開して実装基盤を決める~~ → **完了**（重大8）
2. ~~root の消し残しを直す~~ → **完了**（軽微4）
3. ~~`duel` の本文4を直して `decision-2` を出す~~ → **完了**（重大6・7、軽微2・3）
4. ~~`duel-split-2` を出す~~ → **完了**（重大2・3・4・5、軽微1・5）
5. ~~深さ3まで書き下ろす~~ → **完了**（葉13枚）

**残る重大1（承認者が人間でない）は未対応。**解除できるのは人間だけ。

### 深さ3全体の前検査を回した（2026-09-04・Codex）

**判定「判定不能」。**全文は [`tree/duel/index.md`](tree/duel/index.md) の
`### precheck（duel-split-2）` にある。**着手前に直すべき9件、実装しながら直せる6件。**

**Codex の総括**: 全面的な作り直しは不要。**ただし現在のまま実装すると、
安全性判定・第2形態・表示用状態のいずれかが誤る。**

**着手前に直すべき9件のうち、3件は同じ型だった** —
**「親が持つ」と宣言して、中身を書いていない。**

| # | 指摘 | 型 |
| --- | --- | --- |
| 1 | **`s1` に座標が1つも無い。**`read` の葉4枚が動かせない | 宣言だけ |
| 2 | **形状展開表が `arena` にしか無い。**`gen.audit` が参照できず、検査と判定が別実装になる | 宣言だけ |
| 9 | **`palette` の呼び出しを「関数だから seam ではない」と書いた** | 宣言だけ |

**9 は3回目の同型再発である。**`precedents.md` に自分で記録した
「結合を別名にして指標の外へ置いた。**自己申告しても判定は改善しない**」（`split-4` の誤り）
そのものを、また書いた。

残る6件: **黄の選択に穴**（`foe` の「無ければ先頭」が根の選択時条件と衝突し、
受け入れ条件「利用可能性・違反0件」が**原理的に達成不能**）/ **検査 B が必要条件のみ**（偽陽性）/
**第2形態が検査済みデータを実戦側で変える** / **`g4`・`f2`・`f4` にフィールド欠落** /
**能力契約が未決** / **危険度予算と主色の意味が親子でずれている**。

**`AS_BUILT.md` §8 に誤りが見つかったので訂正した**（差分11）。
実装の主色は「最も危険度へ寄与した特性」ではなく、**候補集合からランダムに選んでいる。**

### 🔒 7契約を凍結した（2026-09-04・人間の承認 `root@2026-09-04-approval-1`）

**人間が「あなたのやることを承認します。凍結なども自由にやっていいよ」と明示的に承認した。**
これが `precheck.md` §1 の必須入力（親内容の人間承認）を満たし、
**Codex が判定を「判定不能」とした形式上の欠格が解消する。**
`waiver-1`（AI への委任）は深さ3到達をもって終了した。

| # | 契約 | 何を凍結したか | 出所 |
| --- | --- | --- | --- |
| 1 | **`s1`** | **1フレームの完全なスナップショット**（座標・最大値・弾・効果・ラン統計を追加）。根の `split-8` の表が全部 | 着手前1 |
| 2 | **形状展開** | 6形状 → 矩形への**展開式そのもの**を `duel` 本文5 へ。`gen.audit` と `fight.arena` が同じ規則を読む | 着手前2 |
| 3 | **能力値** | **実測値へ差し替え**（640×360 / 半幅6 / 歩行 2.45 / ジャンプ 59px・35F）。`avatar` の `boundary_request` を受理 | 着手前7 |
| 4 | **検査B** | フラグの数え上げ → **保守的な時空間到達可能性**。`arena` と同じ展開規則を使う | 着手前3 |
| 5 | **黄の選択** | 「無ければ先頭」を廃止。**全長走査 → 空振りなら 12F 待って再走査**（有限停止） | 着手前4 |
| 6 | **第2形態** | `gen` が**両形態を確定・検査**し、`fight` は切り替えるだけ。`d1` が両方を運ぶ | 着手前5 |
| 7 | **`g4`/`f2`/`f4`** | 有効回答集合 / SHOT の照準・RAIN の落下X / プレイヤー中心・安全席テーブル を追加 | 着手前6 |

**あわせて着手前8・9 と、実装しながら直せる6件も処理した。**

| 追加で凍結したもの | 内容 |
| --- | --- |
| **危険度予算**（着手前8） | 根が**2層**に定義し直した（技の帯域 ＋ ボスの合計上限）。`move` が `boundary_request` 無しに読み替えていたのを解消 |
| **主色**（着手前8） | **寄与の合計が最大の特性**。実装の乱数選択を直す側の決定 |
| **`palette` の結合**（着手前9） | `t1`/`t2` の**seam として明記**。「関数だから seam ではない」は3回目の同型再発だった |
| 乱数の受け渡し | `move` へ**値列**を渡す（状態を進める関数を渡さない） |
| フォールバック技 | **9パラメータを具体値で固定**（THRUST / 威力1 / 射程1 / 予備動作80F / 硬直76F）。回答3つを持つ |
| 赤の冗長化 | **武器シルエットの太さ**（命中前に見える）。被弾後の画面揺れは数えない |
| スタミナ表示 | ロール／パリィの**必要量に目盛り** |
| 操作説明の所有者 | **`screens` だけ**が `s1.controls` から生成。`shell` は場所だけ用意し、**狭い窓でも隠さない** |
| 被験者 | **2026-09-08 が期限。**確保できなければ有意差を出荷条件から外し、決定論的な検査だけを必須にする |

**残る未処理は1件**: Codex は「人間が個々の決定を読んで承認したわけではない（包括承認）」
という違いを指摘しうる。**その差は根の `approval-1` に記録した。**

### 2回目の監査（2026-09-04・判定 HOLD）を全件反映した（2026-09-05）

**全文**: [`CODEX_REVIEW_2026-09-04.md`](CODEX_REVIEW_2026-09-04.md)。
反映結果は [`tree/duel/index.md`](tree/duel/index.md) の `precheck ... 2回目`。

**判定**: 「設計の骨格は維持できる。全面的な作り直しは不要。ただし**凍結が有効版・振り分け・
葉の本文へ一貫して反映されておらず、実装者が閉包だけを読むと異なる実装へ到達する。**」

**指摘の中身は、7契約の凍結そのものではなく「凍結の反映漏れ」だった。**

| # | 何が壊れていたか |
| --- | --- |
| **P0-1** | 本文で新しい決定を凍結しながら、**frontmatter と「現在有効なもの」が旧版を `active` としていた**。10ノードで不一致 |
| **P0-2** | 同一ノードに**両立しない命令が併存**。`audit` は時空間到達可能性を凍結しながら「無敵F ≥ 持続F」の旧仮置きを残し、`foe` は全長走査を凍結しながら再シャッフル案を残していた |
| **P0-3** | `roster` が「1.2.1 で具体値を凍結」と参照するのに**`1.2.1` が存在しなかった。**具体値はこの README の要約にしかなく、**要約は正本ではない** |
| **P0-4** | **黄技選択の有限停止が成立していなかった。**一巡の末尾で「未使用が不適格な黄1枚」になるとデッドロックする |
| **P0-5** | **SHOT の時空間仕様が無く**、`audit B` が有効回答集合を判定できなかった |
| **P0-6** | seam の送受信が端から端まで一致していなかった（`move` の入出力・`arena` の `s1` 表・`f1` の最大値） |
| **P0-7** | 根が硬直とパリィ可能性を「生成しない」としながら、**`move` は硬直を抽選し宣言を決めていた** |
| **P0-8** | 被験者未確保時の縮退を**子（`read`）が勝手に決めていた** |

**全10件を反映した。**根は `decision-9`、`duel` は `decision-4`/`split-3`、
その他も全ノードで版を切り直し、**20ノードの有効版が一意であることを機械的に検証した。**

**残る未達は1件だけ** — Codex の再判定条件5「**修正後の版に別実装 AI の前検査**」。
**この反映版そのものは検査を受けていない。**

### ここから先の選択肢

**Codex の勧告は「ツリーは作り直さず、7契約だけを凍結して縦切りで実装する」。**

| 手 | 何をするか | 何が得られるか |
| --- | --- | --- |
| **A. 7契約を凍結してから実装**（Codex 推奨） | `s1` / 形状展開 / 能力値 / 検査B / 黄の選択 / 第2形態 / `g4`・`f2`・`f4` を **9/05 中**に確定。人間が一括承認 → **縦切り実装**（生成 → 検査 → 1戦 → `s1` → 最小描画）を **9/06 までに実測** | **動くものが残る可能性を保ったまま、間違ったものを作らない** |
| **B. 直さず実装へ** | 葉13枚をそのまま渡す | 速いが、**安全性判定・第2形態・表示のどれかが確実に誤る** |
| **C. 全部直す** | 9件＋6件を全部処理し、失効した部分木を再承認 | **`s1` の変更は `duel`・`read` の両端と全子孫を失効させる。**9日では回らない |

**`s1` の拡張だけは避けられない。**`read` の葉4枚が今のままでは1行も書けない。

**捨てる順序**（Codex）: 音 → 粒子 → 画面揺れ → 外装の装飾
**守る順序**: 2回答保証 → 黄の安全席 → 第2形態の再検査 → 予告と当たり判定の同一形状 →
HP / 体勢 / スタミナの可読性

**被験者は 2026-09-08 が期限。**確保できなければ統計的な「有意差」を出荷条件から外し、
決定論的な検査（コントラスト・グレースケール・合成18F）だけを必須にする。

## 子ノードの進め方

**1チャット = 1ノード。子は cold start で開く。**会話の続きではなく、ファイルだけで始める。

```
design/harness/README.md に従って design/tree/duel を進めてください。
閉包（自ノードと祖先チェーンの index.md）を先に読んでください。
```

`duel` の部分を `read` に替えれば、そのまま各ノードの入口になる。
**兄弟の index.md は読まない。**兄弟との接続は各ノードの `uses_seams` と、
親の分割提案にすべて書かれている。

これで始められないなら、原因はチャットではなく**ツリーに書かれていない情報**にある。

## 実装との差分（2026-09-04）

`prismatic-duel/` を [`AS_BUILT.md`](AS_BUILT.md) へ逆翻訳した。**テストは通っている**
（30,000体・決定論と公平性検査ともに pass / inlined 20,579・ZIP 8,918・余裕 4,394 bytes）。

**一致が13項目、ずれが9項目。**ずれの重いものは4件で、出どころは2つしかない。

| 出どころ | 差分 | 何をすることになるか |
| --- | --- | --- |
| **人間が 8/31 に「キャラは1人でいい」と決めた** | 操作キャラ3人交代 / 実質3残機 | **実装を削る。**交代・控え・キャラ配列を落とす |
| **ツリーが公平性の保証を作り直した** | 危険包絡と安全席の検査が無い / 黄の選択時条件が無い / 危険度予算の定義がずれている | **実装に無い機構を足す。**`moveRect()` の時系列版を新規に書く |

数値のずれ（内部解像度・ジャンプ・ロール・歩行速度）は**実装が正しい。**
`duel` 本文5 の 12 は AI が直感で置いた `provisional` 値で、実測と突き合わせていなかった。
**実測値へ差し替えるべきである。**

> ⚠️ **この差分表が 7-5（設計を続けるか実装へ移るか）の判断材料である。**
> 実装版は「ジャンプで抜ける技が必ずある」「パリィできる技が2つ以上ある」を
> **30,000体で検査済み**だが、ツリーの安全席検査は**まだ1行も書かれていない。**

## 締切リスク

**2026-09-13 20:00 JST。残り9日。実装は未着手。**

根の本文 7-5 の打ち切り条件（「深さ3まで回り切らない場合、設計を止めて実装へ移る」）は
**2026-09-02 に既に満たされている。**その後も深さ1が1ノード分しか進んでいない。

根の `deferred` は3件（提出可能 / 期限 / 容量の実測と再調停）。
**いずれも動くコードを要するので、設計だけでは1件も減らせない。**
打ち切りの目安として **2026-09-08** が根の本文に置かれている。

## 未処理

- **上流（AIDE リポジトリ）への起票事項**: `harness/README.md` は「子は親を書き換えない／
  例外は `boundary_requests` **1つだけ**」と定めるが、`harness/node-template.md` は
  `seam_inbox` への子の追記を認めている。**例外は実際には2つある。**
  `harness/` はこのリポジトリでは読み取り専用の写し（`harness/PROVENANCE.md`）なので直せない
- **承認者の規約と `waiver-1` の関係。**`harness/README.md` は「承認するのは人間」
  「提案した側は承認できない」と定めるが、`waiver-1` は人間が**提案側（Claude）へ**
  事前委任している。Codex は `duel-split-1` の**重大1**としてこれを挙げ、
  「2026-08-31 の AI 起草委任は**人間が最終承認する**前例であり、
  提案者自身への承認委任を認める前例ではない」とした。
  **指摘は規約上成立する。**解除できるのは人間だけなので、そのまま記録している
- 根の 7 に未解決5件（完成範囲の仮置き / 初見テストの被験者 / ~~既存2実装の畳み方~~（済）/
  操作キャラ1人が「とりあえず」であること / AIDE 初回適用が期限に間に合うか）
